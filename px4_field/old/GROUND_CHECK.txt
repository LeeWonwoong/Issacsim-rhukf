════════════════════════════════════════════════════════════════════════
  지상 검증 — 실내 · 프로펠러 제거 상태            (2026-08-04 2차 개정)
  F1 / F2 / F3 를 실외에 나가기 전에 전부 돌려본다
════════════════════════════════════════════════════════════════════════

────────────────────────────────────────────────────────────────────────
[A]  ★ 여기서 되는 것과 안 되는 것 — 먼저 읽을 것
────────────────────────────────────────────────────────────────────────

  ✅ 지상 벤치에서 **검증되는 것** (배선·논리)
     · 오프보드 진입/이탈이 스위치로 깨끗하게 되는가
     · setpoint 가 오프보드일 때만 나가는가 (조종기와 안 싸우는가)
     · 시퀀스 스테이지가 설계한 순서·타이밍대로 도는가
     · **자세 명령이 실제 모터 4채널을 갈라놓는가** (축·부호·순서)
       ← F2 본비행 안전을 좌우한다. [3] STEP 2 참조

  ❌ 지상 벤치에서 **절대 안 나오는 것** (계수)
        C_thrust = m·g / u_hover     ← 실제로 무게를 지탱해야 u_hover 가 생긴다
        G_i      = ω̇_i / cmd_i       ← 실제로 각가속도가 있어야 한다
        drag     = f(v)              ← 실제로 움직여야 한다
     기체를 책상에 올리든 고정하든 u_hover·ω̇·v 가 전부 0 이라
     **분모가 0 이거나 신호가 없어 회귀가 성립하지 않는다.**
     고도를 "맞추는" 문제가 아니라 실제로 떠서 무게를 지탱하고 회전해야
     나오는 값이다. → **계수는 100% 실비행**(FIELD_PROCEDURE.txt).

  ⚠ F3 의 속도 setpoint 경로는 여기서 검증되지 않는다.
    bench 는 속도명령을 자세명령으로 치환한다(offboard_common.py:323).
    실외 F3 첫 진입 때 처음 검증된다.


────────────────────────────────────────────────────────────────────────
[B]  ★ 계수 추출은 ulog 로 한다 — DDS 토픽이 ✗ 여도 괜찮다
────────────────────────────────────────────────────────────────────────

  fit_from_ulog.py:150-155 는 전부 **.ulg 파일**에서 읽는다:
        d_thr = topic(ulog, 'vehicle_thrust_setpoint')
        d_tq  = topic(ulog, 'vehicle_torque_setpoint')

  그리고 그 토픽들은 PX4 **기본 로깅 프로파일**에 이미 들어 있다
  (src/modules/logger/logged_topics.cpp, 전부 add_default_topics() 안):

        sensor_combined              123행
        vehicle_gps_position  10Hz   145행     ← GPS σ 는 이걸 쓸 것
        sensor_gps             1Hz   222행     ← 기본 1Hz. 너무 느리다
        vehicle_thrust_setpoint 50Hz 238행
        vehicle_torque_setpoint 50Hz 239행

  → check_ros2_topics.sh 에서 이 토픽들이 ✗ 로 떠도 **F1/F2/F3 계수 추출에는
    아무 지장이 없다.** DDS 노출은 나중에 젯슨에서 UKF 를 **실시간**으로
    돌릴 때(배포 단계) 필요하다. 지금 단계에서는 아니다.

  실제로 지금 필요한 DDS 토픽은 이 6개뿐이다:
        PUB: vehicle_odometry (또는 vehicle_local_position),
             vehicle_attitude, vehicle_status
        SUB: offboard_control_mode, trajectory_setpoint,
             vehicle_attitude_setpoint


────────────────────────────────────────────────────────────────────────
[C]  터미널 세팅 — 창을 열 때마다
────────────────────────────────────────────────────────────────────────

  ⚠⚠ **`.bashrc` 에서 `colcon build` 를 자동 실행하지 말 것.**
     터미널을 열 때마다 워크스페이스를 다시 빌드해 수십 초씩 잡아먹고,
     빌드가 실패하면 셸 자체가 이상해진다. **소싱(source)만 하면 된다.**
     들어가 있으면 제거 (한 줄):

sed -i '/colcon build/d' ~/.bashrc && source ~/.bashrc

  ▶ 올바른 세팅 — 3줄. ~/.bashrc 에 박기 (한 줄, 1회만)
    ※ `[ -f ]` 가드가 있어 해당 파일이 없는 기계(노트북 등)에서도 조용히 넘어간다

printf '\n# ROS2 / PX4\nexport ROS_DOMAIN_ID=0\n[ -f /opt/ros/humble/setup.bash ] && source /opt/ros/humble/setup.bash\n[ -f ~/colcon_ws/install/setup.bash ] && source ~/colcon_ws/install/setup.bash\n' >> ~/.bashrc && source ~/.bashrc

  ▶ 이번 창에서만 (한 줄)

source /opt/ros/humble/setup.bash && source ~/colcon_ws/install/setup.bash

  ▶ 확인 (한 줄)

echo "DOMAIN=$ROS_DOMAIN_ID" && ros2 topic list | grep -c /fmu/

  ★ 순서: 언더레이(/opt/ros/humble) 먼저, 오버레이(colcon_ws=px4_msgs) 나중.
  ★ `~/colcon_ws` 는 px4_msgs 를 빌드해둔 워크스페이스다. 노트북에 없으면
    위 가드가 건너뛴다. 노트북에서도 ROS2 로 토픽을 받아야 한다면 한 번만:
        mkdir -p ~/colcon_ws/src && cd ~/colcon_ws/src
        git clone https://github.com/PX4/px4_msgs.git
        cd ~/colcon_ws && colcon build --packages-select px4_msgs   ← 이 한 번만
  ★★ 도메인 ID 는 ①이 터미널 ②MicroXRCEAgent 띄운 창 ③PX4 파라미터
     UXRCE_DDS_DOM_ID **세 곳이 전부 같아야** 한다(③은 재부팅 필요).
     하나라도 다르면 /fmu/ 가 통째로 안 뜬다.
     **지금 0 으로 잘 되고 있으므로 0 을 유지하는 편이 안전하다.**


────────────────────────────────────────────────────────────────────────
[1]  준비 — 매번
────────────────────────────────────────────────────────────────────────
  □ 프로펠러 4개 제거  ← --bench-thrust 를 올릴 것이므로 두 번 확인
  □ 기체를 테이블에 고정 (손으로 누르지 말 것)
  □ 시동 중 절대 손대지 말 것 — 모터는 돈다
  □ 창 세 개: 창1=스크립트, 창2=발행 감시, 창3=모터 감시. **모두 [C] 세팅**

  ▶ 최신 코드 확인 (한 줄, 2 가 나와야 함)

grep -c "_keepalive_ocm" ~/px4_field/offboard_common.py

  ▶ 토픽 점검 (한 줄) — ✗ 가 나와도 [B] 를 읽고 판단할 것

cd ~/px4_field && ./check_ros2_topics.sh

  ⚠ 프로펠러가 없으면 기체가 자세 명령에 반응 못 해 자세 오차가 계속 남는다.
    모터 명령이 한쪽으로 밀린 채 커지는 것은 **정상**이다.
    (적분기는 land detector 가 동결시킨다 — mc_rate_control.cpp:220)

  ★ 운용 규칙: **1 스크립트 실행 = 1 오프보드 구간.**
    ✅완료 → 창1 Ctrl-C → 오프보드 스위치 OFF → 시동 해제.  이유는 [4].


────────────────────────────────────────────────────────────────────────
[2]  창 구성 — 세 스텝 공통
────────────────────────────────────────────────────────────────────────

  창2 (setpoint 가 나가는가) — 한 줄

ros2 topic hz /fmu/in/vehicle_attitude_setpoint

  창3 (모터가 실제로 반응하는가) — QGC → Analyze → MAVLink Console 에서

listener actuator_motors

     ※ ROS2 로 보려면 /fmu/out/actuator_motors 를 dds_topics.yaml 에 넣고
       펌웨어를 재빌드해야 한다. 지금은 [5] 를 읽고 판단할 것.
       재빌드했다면:  ros2 topic echo /fmu/out/actuator_motors

  진행 패턴
  ────────────────────────────────────────────────────────────────────
  단계                      창2            창3            창1
  ① 스크립트 실행           조용 ★         —              배너 + "아직 발행 안 함"
  ② Stabilized 로 시동      조용 ★         아이들          (모터 아이들)
  ③ 오프보드 스위치 ON      ~10Hz 시작     값 변함         "★ OFFBOARD 진입"
                                                          nav_state 2 → 14
  ④ 진행                    10Hz 유지      채널별로 갈림   스테이지 순서대로
  ⑤ "✅ 시퀀스 완료"        10Hz 유지      유지            (DONE)
  ⑥ 창1 Ctrl-C             **멈춤** ★     아이들 복귀     "마커 로그 저장"
  ⑦ 오프보드 스위치 OFF     —              —              (인계 완료)
  ⑧ 시동 해제

  ★ ①② 조용 → ③ 10Hz + 모터 변화 → ⑥ 멈춤. 이 셋이 전부다.

  ※ Enter 는 절대 누르지 말 것. 한 번 누르면 stream_armed 가 **영구 래치**되어
    (offboard_common.py:249) 오프보드를 꺼도 setpoint 가 계속 나간다.


────────────────────────────────────────────────────────────────────────
[3]  STEP 1 / 2 / 3
────────────────────────────────────────────────────────────────────────

╔══ STEP 1 — F1 호버 (약 20초) · 진입/이탈 배선 ══════════════════════╗

  창1 (한 줄)

cd ~/px4_field && python3 f1_hover.py --bench --need-alt 0 --dur 20

  확인할 것
    □ 스위치 전 창2 조용
    □ 스위치 ON → 창1 "★ OFFBOARD 진입", nav_state 2 → 14
       진입 로그에 "[BENCH] 추력 0.10" 이 같이 찍힌다
    □ 창2 average rate ≈ 10.0, min/max 편차 작을 것 (링크 안정성)
    □ HOVER 0/20s → 19/20s 까지 1초씩 빠짐없이
    □ "✅ 시퀀스 완료" → Ctrl-C → "마커 로그 저장: field_logs/f1_hover_*.csv"

╚══════════════════════════════════════════════════════════════════════╝


╔══ STEP 2 — F2 doublet (약 25초) · ★ 가장 중요 ══════════════════════╗

  F2 만 PX4 가 고도를 안 잡고 스크립트가 추력을 직접 준다. 실비행 위험이
  제일 크므로 **모터가 실제로 갈리는지**를 여기서 반드시 확인한다.

  창1 (한 줄) — 추력을 올려야 반응이 보인다. **프로펠러 제거 재확인!**

cd ~/px4_field && python3 f2_doublet.py --bench --need-alt 0 --n 2 --settle 2 --bench-thrust 0.22

  확인할 것
    □ 창2 ≈ 10Hz, RECOVER 구간에도 안 끊김
    □ 창1 스테이지가 **이 순서 그대로**:
          SETTLE 0/2s → 1/2s
          roll  1/2 PULSEA +10° → PULSEB -10° → RECOVER
          roll  2/2 …
          pitch 1/2 …  pitch 2/2 …
          yaw   1/2 …  yaw   2/2 …
          DONE_HOLD → ✅ 시퀀스 완료
    □ ★★ 축이 roll 2회 전부 → pitch 2회 → yaw 2회 순인가
       섞이면 실비행 로그에서 축별 계수를 분리할 수 없다
       (sim 에서 조건수 1412 로 롤/피치 분리 실패 전례 — f2_doublet.py 헤더)
    □ ★★ 창3 actuator_motors 4채널:
          roll  PULSEA → 두 채널 ↑ / 두 채널 ↓,  PULSEB → 정확히 반대로
          pitch → 갈리는 짝이 바뀐다
          yaw   → 대각선끼리 짝이 된다
       이게 "setpoint → 컨트롤러 → 모터" 사슬이 이어졌다는 유일한 증거다.

  ⚠ 해석 주의: 기체가 고정돼 자세가 안 변하니 자세 오차가 계속 남는다.
    → P 항이 한쪽으로 밀린 채 유지되고 **크기는 비현실적으로 커진다.**
    **부호·축·순서를 보는 용도이지 크기를 보는 용도가 아니다.**

  ※ 실비행에서는 --thrust 에 F1 로그의 호버 스로틀을 넣는다.
    bench 는 그 값을 무시하고 --bench-thrust 를 쓴다(_alt_hold_thrust:103).

╚══════════════════════════════════════════════════════════════════════╝


╔══ STEP 3 — F3 직선왕복 (약 22초) · 시퀀스 타이밍만 ═════════════════╗

  창1 (한 줄)

cd ~/px4_field && python3 f3_drag.py --bench --need-alt 0 --v 0.5 --leg 3 --reps 1 --pause 2 --settle 2 --bench-thrust 0.22

  확인할 것
    □ 배너에 "비행 범위" 출력 (실외에서 공간 판단에 쓸 화면)
    □ 스테이지 4구간 순서대로:
          SETTLE → 1/4 fwd+1 CRUISE → PAUSE → 2/4 fwd-1 → 3/4 lat+1 → 4/4 lat-1
    □ 창2 ≈ 10Hz

  ⚠ [A] 대로 **속도 setpoint 경로는 여기서 검증되지 않는다.**
    실외 F3 첫 진입 때 창2 를 아래로 바꿔 position=nan / velocity=값 을 확인할 것:

ros2 topic echo /fmu/in/trajectory_setpoint --qos-reliability best_effort --once

╚══════════════════════════════════════════════════════════════════════╝


────────────────────────────────────────────────────────────────────────
[4]  ⚠ 인계 후 조종기 왼쪽 스틱이 안 먹을 때 (2026-08-04 현장 증상)
────────────────────────────────────────────────────────────────────────
  증상: 시퀀스가 끝까지 돈 뒤 Ctrl-C 없이 조종기를 수동으로 되돌리면
        왼쪽 스틱(스로틀)이 안 먹는다.

  원인 후보: /fmu/in/vehicle_attitude_setpoint 는 PX4 내부의 uORB
        `vehicle_attitude_setpoint` 인데 **Stabilized 의 수동 자세 FlightTask 도
        같은 토픽에 발행**한다. bench 스크립트가 쏘는 게 바로 그 토픽이고
        내용은 thrust_body 고정값이다. 두 발행자가 10Hz 로 교대하면
        스로틀이 스틱값과 고정값 사이를 오간다.
        (2026-08-03 에 trajectory_setpoint 로 겪은 것과 같은 종류)

  ▶ 판정 — 수동으로 되돌린 **직후** (한 줄)

ros2 topic hz /fmu/in/vehicle_attitude_setpoint

      · 계속 10Hz  → 스크립트가 범인. Ctrl-C 하면 즉시 정상.
      · 조용함     → 스크립트 무관. PX4/조종기 쪽(모드 매핑·스틱 캘리브)

  ▶ 운용 규칙 (원인 확정 전까지)
      **조종기를 수동으로 되돌리기 전에 창1 을 먼저 Ctrl-C 한다.**

  ※ Ctrl-C 를 해야 하는 다른 이유 둘 (항상 해당)
      · 마커 CSV 는 Ctrl-C(destroy_node) 전까지 디스크에 안 써진다
        (offboard_common.py:143, 462). 하드킬하면 로그가 날아간다.
      · 살려두면 오프보드를 다시 켜는 순간 시퀀스가 **t=0 부터 재시작**한다
        (origin·yaw0 재스냅샷, 405-412행).
        F1 은 타이머만 리셋되지만 **F2 는 doublet 이 예고 없이 다시 발사된다.**


────────────────────────────────────────────────────────────────────────
[5]  펌웨어·토픽 상황 정리 (2026-08-04 조사 결과)
────────────────────────────────────────────────────────────────────────

  ▶ 현재 증상: check_ros2_topics.sh 에서 3개 ✗
        sensor_gps                 → **이름이 틀렸다.** Isaac Sim 쪽 이름이다
                                      (run_sim.py:153 이 /sim/sensor_gps 로 만든다).
                                      실기는 /fmu/out/vehicle_gps_position,
                                      타입은 똑같이 SensorGps. 스크립트는 수정 완료.
        vehicle_thrust_setpoint    → **out 이 맞고 PX4 기본 yaml 7·9행에 이미 있다.**
        vehicle_torque_setpoint      그런데도 0 이면 FC 에 올라간 펌웨어가
                                     그 yaml 로 빌드된 게 아니다.

  ▶ 무엇이 구버전인가: **FC 에 플래시된 PX4 펌웨어**다.
     QGC 는 지상국 앱일 뿐 토픽 이름에 관여하지 않는다.
     dds_topics.yaml 은 파일로 읽히는 게 아니라 빌드 때 dds_topics.h 로
     코드 생성돼 **펌웨어 바이너리에 박힌다.**
     → yaml 을 새로 만들 필요는 없다. 기본 yaml 에 이미 다 있다.

  ▶ FC 버전 확인 — QGC → Analyze → MAVLink Console

ver all

  ▶ 실제로 뭘 내보내는지 (한 줄)

ros2 topic list | grep /fmu/out/ | sort

  ▶ ★ 권고: **지금은 재빌드하지 말 것.**
     1) [B] 대로 C값 목표에 이 토픽들이 필요 없다
     2) 재빌드하면 **토픽 이름이 바뀐다.** PX4 는 MESSAGE_VERSION≠0 인 메시지에
        _v<N> 접미사를 자동으로 붙인다(utilities.hpp:35, dds_topics.h.em:83).
        소스 트리(v1.18.0-alpha1) 기준:
              VehicleStatus            v4  → /fmu/out/vehicle_status_v4
              VehicleAttitudeSetpoint  v1  → /fmu/in/vehicle_attitude_setpoint_v1
              VehicleLocalPosition     v1  → /fmu/out/vehicle_local_position_v1
              TrajectorySetpoint · VehicleCommand · VehicleOdometry ·
              VehicleAttitude · ActuatorMotors  = 0 → 변화 없음
        지금 FC 는 vehicle_status 를 접미사 없이 내보내고 있다(스크립트가
        nav_state 를 읽고 있으므로) → **FC 는 이 트리보다 구버전이다.**
        재빌드하면 offboard_common.py:112·115·118 을 같이 고쳐야 한다.
     3) 현장 나가기 직전에 펌웨어를 갈아엎는 것은 위험을 새로 만든다.

  ▶ 재빌드를 결정했다면
        cd ~/PX4-Autopilot && make list_config_targets | grep -i fmu   # 타겟 확인
        make <타겟>                                                     # 빌드
        QGC → Vehicle Setup → Firmware → Advanced → 커스텀 .px4 선택
     ※ ~/PX4-Autopilot 의 dds_topics.yaml 에는 벤치 관찰용으로
       /fmu/out/vehicle_angular_velocity(주석 해제) 와 /fmu/out/actuator_motors
       가 추가돼 있다(2026-08-04). **빌드 전까지는 아무 효과가 없다.**
       되돌리려면:
        cd ~/PX4-Autopilot && git checkout src/modules/uxrce_dds_client/dds_topics.yaml


────────────────────────────────────────────────────────────────────────
[6]  안 될 때 — 3단 분리
────────────────────────────────────────────────────────────────────────
  1) 창2 가 아무것도 안 뜬다 → 스크립트가 살아있는지부터 (한 줄)

ros2 topic hz /fmu/in/offboard_control_mode

     · 10Hz 뜸  → 통신 정상. 2) 로.
     · 안 뜸    → 두 창이 서로 안 보인다. 양쪽에서 (한 줄)

echo "DOMAIN=$ROS_DOMAIN_ID" && ros2 node list

  2) 스위치를 켜도 nav_state 가 14 로 안 간다
     → QGC 메시지창에 이유가 그대로 찍힌다:
        "Offboard requires local position"    → --bench 를 안 붙였다
        "Offboard requires attitude estimate" → 자세추정 문제. 재부팅/캘리브
        "No offboard signal"                  → OffboardControlMode 미도달 → 1)

  3) 진입은 했는데 setpoint 가 안 나간다
     → 창1 마지막 줄 "위치/속도 수신 대기 중" → 실내면 --bench 로 우회

  ▶ 발행자 확인 (한 줄)

ros2 topic info /fmu/in/vehicle_attitude_setpoint

  ▶ 값까지 보기 (한 줄)

ros2 topic echo /fmu/in/vehicle_attitude_setpoint --qos-reliability best_effort --once

    ※ hz 에는 --qos-reliability 가 **없다**(hz.py 가 best_effort 하드코딩).
      붙이면 unrecognized argument 로 죽는다. echo 는 붙이는 게 안전하다.


────────────────────────────────────────────────────────────────────────
[7]  전부 통과하면 — 실외로
────────────────────────────────────────────────────────────────────────
  □ STEP 1/2/3 통과 (특히 STEP 2 의 모터 4채널 부호·축·순서)
  □ QGC → MPC_THR_HOVER 를 0.5 → 0.33 (FIELD_PROCEDURE 1-4)
  □ 펌웨어는 **그대로 두고** 나가는 것을 권장 ([5])

  실외 순서는 FIELD_PROCEDURE.txt.
  **F1 → 로그검사 → F2 짧게(4-A) → 로그검사 → F2 본비행(4-B) → F3.**
  F1 의 호버 스로틀을 F2 --thrust 에 넣어야 하므로 순서를 바꿀 수 없다.
  계수는 전부 ulog 에서 fit_from_ulog.py 로 뽑는다.


────────────────────────────────────────────────────────────────────────
[8]  수정 이력
────────────────────────────────────────────────────────────────────────
  2026-08-04(2) · [A] 벤치에서 되는 것/안 되는 것 명시 — 계수는 정지 상태에서
                  원리적으로 안 나온다(책상에 올려도 동일).
                · [B] 계수 추출은 ulog 경로임을 명시. DDS ✗ 여도 무관.
                · [5] 펌웨어/토픽 상황 + 재빌드 시 _v<N> 접미사 경고 추가.
                · --bench-thrust 플래그, 창3(actuator_motors) 모터 관찰 추가.
  2026-08-04(1) · 실내 전용 재작성, F1/F2/F3 3스텝. 터미널 세팅 흡수
                  (TERMINAL_SETUP.txt 삭제). [4] 왼쪽 스틱 증상 추가.
  2026-08-03    · _tick 이 항상 OCM(position=True) 를 발행하던 버그 수정
                  (_keepalive_ocm). bench 가 위치 수신을 기다리던 버그 수정.
                  검증: python3 _test_flow.py
════════════════════════════════════════════════════════════════════════
