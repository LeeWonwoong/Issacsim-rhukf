════════════════════════════════════════════════════════════════════════
  px4_field 구 문서 보관함                                (2026-08-07 갱신)
════════════════════════════════════════════════════════════════════════

  ⚠ 여기 있는 문서는 **지금 따르면 안 된다.**

     현행 실기 절차   →  px4_field/RUN.txt          (F1~F8, AUW 1.340)
     현행 Isaac 리허설 →  px4_field/ISAAC_F1_F8.txt  (수동조종 F1~F8)

  ⚠⚠ 가장 위험한 차이: **비행 번호 체계가 바뀌었다.**
        구:  F1 호버 / F2 doublet / F3 drag
        신:  F1 호버 / F2 오토튠 / F3 doublet / F4 drag / F5~F8 기동패턴
     여기 문서는 전부 **구 번호**다. 현장에서 이걸 보면 F2/F3 을 잘못 난다.


  ── 2026-08-07 에 옮긴 것 (F1~F8 재번호 + 제어기 제한 주입으로 폐기) ──

  FIELD_PROCEDURE.txt
      구 F1/F2/F3 실외 수집 절차. AUW 1.342, 구 번호.
      → 대체: RUN.txt. 비행별 상세·판정 기준까지 RUN.txt 에 들어갔다.

  GO_FIELD.txt
      2026-08-04 "출발 전 준비". 짐 목록·지상 파라미터.
      → 대체: RUN.txt 의 "나가기 전 [지상, QGC]" 절.

  GROUND_CHECK.txt
      2026-08-04 실내·프로펠러 제거 지상검증(STEP 1/2/3).
      → 대체: ISAAC_F1_F8.txt. 프로펠러 뺀 벤치가 답 못 하던 **비행 거동**을
        SITL 수동조종으로 본다. 벤치 검증 자체가 필요하면 이 문서를 참고할 것
        (--bench 플래그는 아직 살아 있다).

  UPGRADE_TO_1.16.2.txt
      PX4 v1.15.4 → v1.16.2 업그레이드 절차. **2026-08-06 에 완료됐다.**
      → 롤백하거나 새 기체를 세팅할 때만.


  ── 그 전부터 있던 것 (v1.15.4 시절) ──────────────────────────

  REAL_AIRFRAME_DDS_SETUP.txt
      v1.15.4 + dds_topics.yaml 2줄 펌웨어 플래시 절차.
      ★ 가장 위험한 문서다 — 이걸 보고 구우면 1.16.2 업그레이드가 되돌아간다.
      → 롤백할 때만 볼 것.

  DDS_TOPICS_SETUP.md
      uXRCE-DDS 토픽 노출 설명. v1.15.4 기준.
      1.16 은 메시지 버저닝(_v4/_v1 접미사)이 들어가 구성이 다르다.
      → 같은 내용이 UPGRADE_TO_1.16.2.txt [2-2] 에 1.16.2 기준으로 있다.


  ── 백업 파일 (.bak_*) ────────────────────────────────────────
    RUN.txt.bak_0805 / .bak_auw1326        RUN.txt 의 08-05 / AUW 1.326 판
    FIELD_PROCEDURE.txt.bak_auw1326        등등 — 이름의 접미어가 무엇을 되돌리는지 알려준다
    offboard_common.py.bak_prepattern      f5_pattern 지원 추가 전
    autotune_go.py.bak_ros2cmd             ROS2 명령 212 를 쏘던 구현(동작 안 함)
    fit_autotune.py.bak_wrongstate         상태 판정 버그판


  ── 옮기지 않은 것 (여전히 현행) ──────────────────────────────
    RUN.txt                 실기 절차 F1~F8                      ★
    ISAAC_F1_F8.txt         Isaac 수동조종 리허설 F1~F8          ★
    MANUAL_FLIGHT_SETUP.md  조종기·QGC Joystick 설정 (버전 무관)
    THRUST_MODEL.md         추력 모델 실측 기록 (절차서가 아니라 데이터)
    firmware/README_WHICH_FILE.txt   어느 .px4 를 구울지
════════════════════════════════════════════════════════════════════════
