════════════════════════════════════════════════════════════════════════
  실기체 DDS 토픽 추가 — 이미 있는 PX4-Autopilot 에 yaml 2줄
                                                   (2026-08-04)
════════════════════════════════════════════════════════════════════════

목적: 제어입력 u(추력·토크 setpoint)를 ROS2 토픽으로 받는다.

╔══════════════════════════════════════════════════════════════════════╗
║  ★★ 결론: 빌드하지 말고 아래 펌웨어를 그냥 플래시하면 된다 ★★        ║
║                                                                      ║
║      px4_field/firmware/px4_fmu-v6c_v1.15.4_dds.px4    (1.8 MB)      ║
║      md5: 75f51a649ea6042facc968cd05aac68c                           ║
║                                                                      ║
║  이 저장소에 들어 있으므로 노트북에서 git pull 만 하면 받아진다.      ║
║  USB 도 빌드도 필요 없다.  →  바로 [4] 플래시로 갈 것.               ║
║                                                                      ║
║  · 내용   = PX4 v1.15.4 + dds_topics.yaml 2줄 (아래 [0])             ║
║  · 커밋   = 99c40407  ← FC 의 ver all git-hash 와 정확히 동일         ║
║  · 보드   = board_id 56 = FMU-v6C                                    ║
║  · 노트북 파일을 하나도 안 건드린다 (QGC 가 파일 하나를 읽을 뿐)      ║
╚══════════════════════════════════════════════════════════════════════╝

  [1]~[3] 은 직접 빌드하고 싶을 때만 본다.

  ※ 급하지 않다. 계수 추출(F1/F2/F3)은 ulog 경로라
    (fit_from_ulog.py:150-155, 기본 로깅 50Hz logged_topics.cpp:238-239)
    플래시 전에도 실외 비행은 그대로 가능하다.

  ※ 도메인 ID 는 0 을 유지한다(2026-08-04 결정). FC 파라미터
    UXRCE_DDS_DOM_ID 도 0 이므로 건드릴 것이 없다.


────────────────────────────────────────────────────────────────────────
[T]  노트북에서 이 폴더 받기
────────────────────────────────────────────────────────────────────────
  이미 저장소가 있으면:

cd <저장소경로> && git pull

  없으면:

git clone git@github.com:LeeWonwoong/Issacsim-rhukf.git

  펌웨어 무결성 확인:

md5sum px4_field/firmware/px4_fmu-v6c_v1.15.4_dds.px4

    → 75f51a649ea6042facc968cd05aac68c  이면 온전하다.

  git 을 못 쓰는 상황이면 USB 로 그 파일 하나만 옮겨도 된다.


────────────────────────────────────────────────────────────────────────
[0]  추가할 토픽 — 딱 2개
────────────────────────────────────────────────────────────────────────
  v1.15.4 기본 yaml 을 실제로 파싱해 확인한 결과:

    sensor_combined          이미 있음   IMU(자이로+가속도). UKF 관측 z[6:9]
    vehicle_gps_position     이미 있음   UKF 관측 z[0:6]
    vehicle_attitude         이미 있음
    vehicle_odometry         이미 있음
    vehicle_local_position   이미 있음
    vehicle_status           이미 있음   nav_state (오프보드 감지)
    ─────────────────────────────────────────────────────────
    vehicle_thrust_setpoint  ★ 추가      UKF u[0]
    vehicle_torque_setpoint  ★ 추가      UKF u[1:4]

  넣지 않는 것
    actuator_motors           제어 할당기 "출력"이라 UKF 모델에 안 들어간다
    vehicle_angular_velocity  자이로가 sensor_combined 에 이미 있어 중복

  ⚠ FLASH 여유가 1% 뿐이다 (2개 추가 후 98.85% = 1943500 B / 1920 KB).
    여기서 토픽을 더 넣으면 빌드가 깨질 수 있다.

  ★ px4_msgs 는 손대지 않는다. colcon build 다시 안 돌려도 된다.
    두 메시지 타입은 px4_msgs 에 이미 있고, yaml 편집은 "기존 타입을 DDS
    목록에 적는 것"일 뿐 새 타입을 만드는 게 아니다.


────────────────────────────────────────────────────────────────────────
[1]  ★ 첫 단계 — 지금 있는 PX4-Autopilot 의 버전 확인
────────────────────────────────────────────────────────────────────────
  아래 두 줄을 그대로 붙여넣는다 (한 줄씩):

git -C ~/PX4-Autopilot log -1 --format='hash: %H'

git -C ~/PX4-Autopilot describe --tags --always

  ※ `git describe --tags` 만 쓰면 태그가 없는 클론에서 아무것도 안 뜬다.
    `--always` 를 붙이면 항상 뜬다. 경로가 다르면 ~/PX4-Autopilot 을 고칠 것.

  FC 가 `ver all` 로 보고한 값 (2026-08-04 실측):
      PX4 version : 1.15.4
      PX4 git-hash: 99c40407ffd7ac184e2d7b4b293f36f10fe561ef

  ┌──────────────────────────────────────────────────────────────────┐
  │ hash 가 99c40407... 로 시작 → 그대로 쓴다.  [2] 로.               │
  │ 다르다 → 아래 (B) 참조                                            │
  └──────────────────────────────────────────────────────────────────┘

  (B) 버전이 다를 때 — 셋 중 하나

    B-1. 이미 빌드해둔 .px4 를 쓴다  ← 가장 간단 (빌드 불필요)
         GPU 워크스테이션에 v1.15.4 + yaml 2줄로 빌드한 펌웨어가 있다:
             ~/px4_fmu-v6c_v1.15.4_dds.px4        (1.8 MB, 2026-08-04)
         .px4 는 아키텍처 무관한 펌웨어 이미지라 어느 PC 에서 만들든 같다.
         USB 로 노트북에 옮기고 [2][3] 건너뛰어 [4] 플래시로 간다.

    B-2. 같은 저장소에서 v1.15.4 를 꺼내 직접 빌드한다 (다운로드 없음)
         태그가 이미 로컬 히스토리에 있으므로 새로 받을 게 없다.
         기존 체크아웃(현재 버전)도 건드리지 않는다.

cd ~/PX4-Autopilot && git worktree add ~/PX4-v1154 v1.15.4

cd ~/PX4-v1154 && git submodule update --init --recursive

         이후 [2][3] 을 ~/PX4-v1154 경로로 수행.
         ⚠ 서브모듈까지 받으면 약 1.1 GB 를 쓴다. 빌드가 끝나면 .px4 만
           밖으로 빼고 지우는 편이 낫다:

cp ~/PX4-v1154/build/px4_fmu-v6c_default/px4_fmu-v6c_default.px4 ~/

cd ~/PX4-Autopilot && git worktree remove --force ~/PX4-v1154 && git worktree prune

    B-3. 그냥 그 버전으로 빌드한다  ← 권하지 않음
         v1.16+ 는 토픽명에 _v<N> 을 붙이고(utilities.hpp:35),
         마이너 점프면 파라미터·캘리브레이션·제어기 기본값이 바뀔 수 있다.


────────────────────────────────────────────────────────────────────────
[2]  yaml 편집
────────────────────────────────────────────────────────────────────────
nano ~/PX4-Autopilot/src/modules/uxrce_dds_client/dds_topics.yaml

  6행이 `publications:` 이고 7행이 빈 줄이다. 그 사이에 아래 4줄을 넣는다.
  (# 로 시작하는 주석 줄은 넣어도 되고 안 넣어도 된다)

  ── 편집 후 파일 앞부분이 이렇게 되면 된다 ────────────────

    #####
    #
    # This file maps all the topics that are to be used on the uXRCE-DDS client.
    #
    #####
    publications:
      - topic: /fmu/out/vehicle_thrust_setpoint
        type: px4_msgs::msg::VehicleThrustSetpoint
      - topic: /fmu/out/vehicle_torque_setpoint
        type: px4_msgs::msg::VehicleTorqueSetpoint

      - topic: /fmu/out/register_ext_component_reply      ← 원래 있던 줄
        type: px4_msgs::msg::RegisterExtComponentReply

  ─────────────────────────────────────────────────────────

  ★ 들여쓰기 엄수:  "- topic:" 은 스페이스 2칸,  "type:" 은 4칸.
    YAML 이라 탭을 쓰면 빌드가 깨진다.

  ▶ 문법 검증 — 빌드 전 30초로 잡는다 (그대로 붙여넣기)

python3 -c "import yaml,os;f=os.path.expanduser('~/PX4-Autopilot/src/modules/uxrce_dds_client/dds_topics.yaml');p=[x['topic'] for x in yaml.safe_load(open(f))['publications']];[print(('OK  ' if t in p else 'MISS'),t) for t in ['/fmu/out/vehicle_thrust_setpoint','/fmu/out/vehicle_torque_setpoint']];print('total',len(p))"

    둘 다 OK 여야 한다.
    ScannerError 가 나면 들여쓰기/탭 문제다.


────────────────────────────────────────────────────────────────────────
[3]  빌드 — 예전에 쓰던 방식 그대로
────────────────────────────────────────────────────────────────────────
cd ~/PX4-Autopilot && make px4_fmu-v6c_default

  툴체인(arm-none-eabi-gcc)이 없다면 한 번만:
      bash ./Tools/setup/ubuntu.sh --no-sim-tools
      (하드웨어 빌드만. 끝나면 dialout 그룹 적용 위해 로그아웃 후 재로그인)

  ▶ 반영 확인

grep -c "ORB_ID(vehicle_thrust_setpoint)" ~/PX4-Autopilot/build/px4_fmu-v6c_default/src/modules/uxrce_dds_client/dds_topics.h

    1 이상이면 반영된 것이다.
    ※ v1.15.4 템플릿은 토픽 문자열이 아니라 ORB_ID(...) 로 들어간다.
      "/fmu/out/..." 로 grep 하면 안 잡히니 위 형태로 볼 것.

  산출물:  build/px4_fmu-v6c_default/px4_fmu-v6c_default.px4


────────────────────────────────────────────────────────────────────────
[4]  플래시
────────────────────────────────────────────────────────────────────────
  ① 파라미터 백업 먼저
       QGC → Parameters → 우상단 Tools → Save to file
       (같은 버전이라 유지되는 게 정상이지만 보험)

  ② 플래시
       1. FC 를 USB 로 연결
       2. QGC → Vehicle Setup → Firmware
       3. USB 를 뽑았다 다시 꽂으라는 안내가 뜨면 그렇게 한다
       4. 우측 패널 Advanced settings → Custom firmware file...
       5. 아래 파일 선택 → 완료 후 재부팅

           px4_field/firmware/px4_fmu-v6c_v1.15.4_dds.px4

         (직접 빌드했다면 build/px4_fmu-v6c_default/px4_fmu-v6c_default.px4)

  ③ 파라미터 확인 — 백업과 대조
       MPC_THR_HOVER / COM_RC_IN_MODE / UXRCE_DDS_DOM_ID / 캘리브레이션
       같은 v1.15.4 라 전부 유지되는 것이 정상이다.

  ④ 버전 확인 — QGC → Analyze → MAVLink Console

ver all

       PX4 version 1.15.4 / git-hash 99c40407... 로 플래시 전과 똑같이
       나오는 것이 정상이다. yaml 은 git 해시에 들어가지 않는다.

  ※ 도메인 ID 는 0 유지 — FC 파라미터도 그대로 두면 된다.
    (0 이 아닌 값을 쓰려면 터미널 · MicroXRCEAgent · UXRCE_DDS_DOM_ID
     세 곳을 동시에 바꾸고 FC 를 재부팅해야 한다)


────────────────────────────────────────────────────────────────────────
[5]  확인 — 토픽 받아보기
────────────────────────────────────────────────────────────────────────
  FC 와 ROS2 를 잇는 다리(MicroXRCEAgent)가 필요하다.

  ▶ 노트북에서 USB 로

MicroXRCEAgent serial --dev /dev/ttyACM0 -b 921600

  ▶ 젯슨에서 (기체 탑재 시)

MicroXRCEAgent serial --dev /dev/ttyUSB0 -b 921600

  ▶ 다른 창에서

ros2 topic list | grep -E "thrust_setpoint|torque_setpoint"

ros2 topic echo /fmu/out/vehicle_thrust_setpoint --qos-reliability best_effort

  ※ 젯슨 쪽 코드는 아무것도 안 고친다. FC 가 내보내기 시작하면 ROS2
    그래프에 그냥 나타난다.

  ※ 토픽 이름만 보려면 px4_msgs 없이도 되지만, echo 로 내용까지 보려면
    그 기계에 px4_msgs 가 빌드돼 있어야 한다.


────────────────────────────────────────────────────────────────────────
[6]  플래시 전에 값만 보고 싶다면
────────────────────────────────────────────────────────────────────────
  QGC → Analyze → MAVLink Console:

listener vehicle_thrust_setpoint

listener vehicle_torque_setpoint

  출력 예 (SITL 실측):
      TOPIC: vehicle_torque_setpoint
         timestamp: 17228000 (0.000000 seconds ago)
         xyz: [-0.00385, 0.00299, 0.00003]

  uORB 값을 그대로 찍어준다. 조종기 조종 중에도, 오프보드 중에도 실시간으로.
  ROS2 토픽이 아닐 뿐 값은 완전히 동일하다.


────────────────────────────────────────────────────────────────────────
[7]  되돌리기
────────────────────────────────────────────────────────────────────────
  QGC → Firmware 에서 표준 PX4 v1.15.4 를 다시 플래시하면 원복된다.
  필요하면 백업한 .params 를 Tools → Load from file 로 되돌린다.
  어느 시점에도 기체가 못 쓰게 되는 상태는 없다.


────────────────────────────────────────────────────────────────────────
[8]  왜 재플래시가 필요한가 / 무엇이 바뀌는가
────────────────────────────────────────────────────────────────────────
  dds_topics.yaml 은 FC 가 런타임에 읽는 설정 파일이 아니다. 빌드할 때
  generate_dds_topics.py 가 dds_topics.h 로 코드 생성해 펌웨어 바이너리에
  컴파일해 박는다. FC 안에 이 파일은 존재하지 않는다.

  표준 v1.15.4 대비 바뀌는 것은 이 yaml 파일 하나(7줄)뿐이다.

    안 바뀜   비행 코드 전부 (제어루프·믹서·제어 할당기·EKF2·land detector)
              파라미터 (같은 버전 = 마이그레이션 없음)
              캘리브레이션 (가속도계·자이로·나침반·RC)
              기존 토픽 이름 (v1.15 는 메시지 버저닝 전이라 _v<N> 없음)
              ulog 로깅
              젯슨 코드 · 조종기 · QGC 설정

    바뀜      /fmu/out/vehicle_thrust_setpoint 발행 시작
              /fmu/out/vehicle_torque_setpoint 발행 시작
              FLASH 98.85% (여유 1%)


────────────────────────────────────────────────────────────────────────
[9]  Isaac SITL 은 완전히 별개
────────────────────────────────────────────────────────────────────────
  GPU 워크스테이션의 ~/PX4-Autopilot (v1.18) 은 Isaac 전용이고 FC 와 무관하다.
  2026-08-04 에 yaml 추가 + make px4_sitl_default 완료했고, 거기엔
  actuator_motors 와 vehicle_angular_velocity 도 넣어뒀다(벤치·시뮬 디버깅용).
  실기엔 넣지 않는다.
════════════════════════════════════════════════════════════════════════
