# sim 정합 — 실기 ↔ Isaac ↔ UKF (상시 참조, 단일 출처)

> **이 문서가 물리값·정합의 단일 출처다.** 날짜 없는 상시 참조본.
> 이력본: `SIM_ALIGN_CHANGES_20260812.md`, `_20260811.md` (이 문서로 대체됨, 이력 보존).
> 서사·로드맵·실험기록: `CLAUDE.md`, `RESULTS_LOG_*.md`.

---

## 0. 왜 정합하나 — 목적과 철학 (먼저 읽을 것)

**최종 목표는 실기체(Q250) inference.** 그래서 정합의 판단 기준은 "모델 정확도"가 아니라
**"모델오차 수준의 현실성"** 이다. 이유의 사슬:

```
정책이 먹는 것 = NIS(잔차)  →  NIS 크기 = 모델오차 크기  →  sim 모델오차가 실기와 같아야 전이
```

그래서 **세 축(① 실기 플랜트 ② Isaac 플랜트 ③ UKF 모델)을 동시에 정합**한다:
- ③ UKF `calibration.json` 은 **sim UKF 와 실기 UKF 가 공유** → 한쪽만 맞으면 반드시 다른 쪽이 깨진다.
- ① 과 ② 가 다르면 sim 에서 뽑은 밴드·임계·d′ 가 실기로 안 넘어간다.

### 핵심 설계 원칙 두 개 (혼동 금지)
1. **틀린 물리는 고친다** (질량·프레임·C_torque·G·drag). 실기에서도 제대로 캘리브할 것이므로 양쪽 다 맞춘다.
2. **실기에서 못 맞추는 미세항은 sim 에서도 "완벽히" 맞추지 않는다.** 안 그러면 모델오차가 sim 에서만
   0 이 되어 평시 NIS 바닥이 **비현실적으로 낮아지고**, 실기에서 상시 오탐이 난다.
   → 즉 **적당한 모델오차를 남기는 것이 현실성**이다. (예: 캘리브 ±5%, 추력-토크 결합의 페이로드 변동)

### R 은 정합 대상이 아니다 (탐지 설계값)
UKF 의 R·Q 는 **센서 σ 에 맞추는 값이 아니다.** 이건 shadow UKF(탐지 필터)라 일부러 **R↑·Q↓(고집)** 로
정상 명령 기반 예측을 더 믿게 만들어 공격 δ 가 잔차로 드러나게 한다. R_gyro 0.2 는 실기 σ²(0.0044)의
~45배 — 의도된 고집. **센서 σ 는 sim "측정"에 먹이고(현실성), R 은 설계값으로 고정.** (0장 원칙 2와 별개 축.)

---

## 1. 캘리브레이션 진화 — old → new, 왜 바꿨나

과거 측정치는 **틀린 플랜트** 위에서 잰 것이라 전부 무효화됐다. 아래가 그 이력과 근거다.

| 물리량 | old (틀림) | new (현행) | 왜 틀렸나 / 왜 바꿨나 |
|---|---|---|---|
| **질량** | 1.6186 (또는 1.5 하드코딩) | **1.340 kg** | 로터 4개(별개 강체) 질량을 빼먹음. 실기는 **저울 실측**(케이블 포함). |
| **C_torque_xy** | 0.265 | **G 193/155** (C_torque_x 3.261·y 3.10) | 구값은 폐루프 rate loop 회귀라 **14배 과소**. 최대 롤각가속 9 vs 실측 52 rad/s² → 기동을 원리적으로 예측 못 함 = "기동↔공격 aliasing" 상당부분이 이 오류. 실기 **오토튠 G**로 재측정. |
| **C_torque_z** | 4.907 → 4.41 | **1.378** (→sim 1.195) | 실기 오토튠 log22 물리게인 Σb/dt 37.3 × Izz. yaw 권한이 −13%. |
| **C_thrust** | 22.82 | **40.3** | 구값은 가정질량 1.5 하드코딩 적합. THR_MDL_FAC 0.78 로 호버 0.326 정합(실기 0.33). |
| **오일러 프레임** | ENU/FLU 공식 → NED 소비 | **NED/FRD 수정** | 추력벡터를 틀린 수평방향 회전 → 기동 중 vel NIS 오염 + 호버전환 90° 요슬루 인공물. |
| **MOTOR_TAU** | 0 (즉응) | **0.014** | Pegasus 는 명령 즉시 로터속도 적용(무지연). 실기 τ≈23ms(오토튠 a2 .62~.69, 3경로 일치). sim↔real 회전동역학 차이의 주범이었음. |
| **결합 K** | 없음 → [-.0012,+.0062,+.0001] | **[+.00182,+.01210,-.00195]** | 실기 11소티 호버트림 실측. sim 은 COM 이동으로 같은 K 를 기하 재현. |
| **drag** | [0.50,0.30,0] | **[0.35,0.40,0]** | 실기 F4 등속법 x .352 / y .397 (x 미확정). |

**요지**: 구 캘리브는 (질량 오류 + C_torque 14배 + 프레임버그)로 **sim 이 실기처럼 안 날았고**, 그 위에서
잰 NIS·밴드·d′ 가 전부 무효였다. 신 캘리브는 **실기 실측**(저울·오토튠·호버트림·drag법)에 grounded →
sim 플랜트 = 실기 = UKF 3자 일치 → **sim 에서 뽑은 것이 실기로 전이**된다.

### ⚠⚠ 함정 — `iris.py` 는 죽은 코드
`run_sim.py` 는 `IrisConfig` 가 아니라 **`MultirotorConfig()` 를 직접** 만든다(`run_sim.py:227`).
`iris.py`·`ROTOR_K` 편집은 플랜트에 **안 닿는다**. 로터/항력의 유일한 진짜 지점 = `run_sim.py` `config_multirotor` 블록.

### 왜 COM 까지 건드리나
`torque_thrust_coupling`(K)은 기하 상수. UKF 에 실기 K 를 넣었으면 sim 플랜트 기하도 같아야 한다.
`calibration.json` 은 sim·실기 UKF 공유 → 이 정합 없으면 sim 에서만 보정이 틀려 평시 NIS 가 **반대로 오른다**.
```
τ = r̄ × F, r̄ = 로터중심 − COM  →  K_roll = −ȳ, K_pitch = +x̄  →  COM = 로터중심 − (K_pitch, −K_roll, 0)
⚠ K 는 기하토크, 트림은 반대부호 상쇄명령 → K = −τ_trim·C/T (트림 그대로 넣으면 gyro NIS 4배)
⚠ COM y 부호: 로터중심 ENU/FLU vs 유도 FRD (FRD y = −FLU y) → COM_FLU=(cx−K_pitch, cy−K_roll, cz)
⚠ 로터 질량희석: r̄_system ≈ 0.911·r̄_body (로터 별개강체) → mass_frac 로 과보정. body_view.set_coms() 필수.
```

---

## 2. 3자 현재 상태 (요약)

```
             ① 실기 Q250              ② Isaac 플랜트              ③ UKF (calibration.json)
질량          1.340 kg                1.340                      1.340                     ✅
호버 u        0.33                    0.326 (THR_MDL_FAC 0.78)   C_thrust 40.3             ✅
G roll        192.5                   physical 192.5             C_torque_x 3.261 → 193    ✅
G pitch       155~164                 155                        C_torque_y 3.10  → 155    ✅
G yaw         37.3                    ROTOR_CM 7.2e-7 → 1.195    C_torque_z 1.378 → 37.3   ✅ (−13%)
회전지연 τ     23ms (a2 .62~.69)       MOTOR_TAU 0.014            (UKF엔 지연 없음)          ✅
drag          x .352 / y .397         DRAG_XYZ 0.35,0.40,0       drag [0.35,0.40,0]        ⬜ x미확정
결합 K        [+.0018,+.0121,-.0020]  COM_ALIGN → 트림 부호일치    torque_thrust_coupling    ✅ (26%)
평시 NIS      gyro .60~1.17 vel .18~.41   gyro 0.84  vel 0.14      (동일 Q·R 로 재구동)      ✅ 범위 안
센서 σ        gyro [.066,.070,.044]   ✅ 주입됨(2026-08-20)         (R 은 설계값, 정합 대상 아님)
              GPS vel [.088,.077,.243]  gyro full·GPS 0.9× UKF단
```

---

## 3. 검증 — 실기 로그로 shadow UKF 재구동 (평시 NIS 바닥 5~10배 ↓, Q·R 불변)

도구: `px4_field/nis_from_ulog.py` — sim 과 같은 Q·R·같은 압축으로 실기 NIS 재계산.

| 비행 | vel NIS p50 | gyro NIS p50 | 압축 gyro p50 |
|------|-------------|--------------|---------------|
| f11_hover | 4.28 → **0.18** | 6.08 → **0.60** | 0.662 → **0.321** |
| f7_waypoint | 1.69 → **0.38** | 5.03 → **1.14** | 0.643 → **0.432** |
| f8_circle | 5.02 → **0.29** | 6.45 → **0.67** | 0.668 → **0.339** |
| f10_aggressive | 2.43 → **0.41** | 4.86 → **1.17** | 0.639 → **0.437** |

축별 자이로 잔차 RMS 원인 일치: roll↑=K_roll, pitch↑(−64%)=K_pitch, yaw↑(−76%)=C_torque_z.
부수효과: 압축 여유 회복(0.64~0.68 → 0.32~0.44, 위쪽 여유 0.25→0.5+).

### sim 검증 3회 (`run_align_verify.sh`) — 세 항목 통과
| 항목 | sim | 실기 | 판정 |
|------|-----|------|------|
| 호버트림 roll/pitch | −0.0054 / −0.0409 | −0.0073 / −0.0554 | ✅ 부호일치 26% |
| C_torque_z | 1.195 | 1.378 | ✅ −13% (목표 ±20% 잠정) |
| 평시 NIS vel/gyro p50 | 0.14 / 0.84 | 0.18~0.41 / 0.60~1.17 | ✅ 범위 안 |

yaw 트림 불일치(sim −0.00098 vs 실기 +0.00417)는 로터 항력 비대칭이라 COM 으로 못 고침. 크기 작아 수용.

---

## 4. 실기 실측치 (근거 데이터, 08-12 11소티)

| 물리량 | 값 | 출처 |
|--------|-----|------|
| G 물리 Σb/dt | roll 192.5 / pitch 164.0 / yaw 37.3 | 오토튠 log22 |
| a2 (모터지연) | .688 / .677 / .623 (τ≈23ms) | 동일 |
| 호버트림 τ_norm | [−0.0073, −0.0554, +0.0042] | 11소티 중앙(σ 14%) |
| drag | x .352 / y .397 (등속) | F4 log28 (x 미확정) |
| C_thrust | 40.170 | F4 log28 |
| **자이로 σ** | **[.066, .070, .044] rad/s** | f11_hover log33 (raw) |
| **GPS 속도 σ** | **[.088, .077, .243] m/s** | 동일 (호버=참속도 0) |
| GPS 레이트 | 10 Hz | vehicle_gps_position |

---

## 5. 최근 (2026-08-20) — 센서 노이즈 & 모델오차 현실화

### 5-1. 센서 σ 주입 (구현됨) — 하지만 gyro 탐지엔 무영향
- 실기 σ 를 sim 측정에 주입: gyro `[.066,.070,.044]` full, GPS 수평 0.9×, GPS 수직 0.219 (raw 일관, 융합 X).
- **UKF단 주입**(online_rl `_run_ukf_step`), 전역 스케일 env `SENSOR_NOISE_SCALE` (clean0/real1/stress2·3).
- **검증**: gyro NIS 거의 안 변함 (R_gyro 0.2 >> σ² 0.0044 + 바람 지배). Q/R 재튜닝해도 최적 R0.2/Q5e-3 불변.
- **함의**: 실기 센서노이즈는 탐지를 안 바꾼다. "센서노이즈 강건성"은 약한 축.

### 5-2. ★ gyro 가 기동에 안 튄다 — 모델오차 현실화 필요 (진행 중)
- **발견**: 평시 gyro NIS p90 = 0.1~0.37 (전 패턴, aggressive 포함). **기동이 gyro aliasing 을 안 만든다.**
  (앞서 "p90 120"은 공격 회복구간 오염이었음. windthreshold bias0 로 확인. plot: maneuver_gyro_check.png)
- **원인**: UKF 캘리브 = sim 플랜트 캘리브 **동일**(모델오차 0) + 순한 기동(ω̄0.2, MPC2.0캡) → gyro 부자연스럽게 깨끗.
  = **0장 원칙 2 위반**(모델오차가 sim 에서만 0). 센서노이즈와 정반대 방향의 비현실성.
- **문헌 정당성**: 기동 중 filter innovation 상승은 정설(IMM 필터·INS 스케일팩터/오정렬·기동탐지). 실기 탐지필터는
  완벽 캘리브 불가 → 기동서 gyro innovation ↑. 우리 sim 이 안 그런 게 비정상.
- **해법**: `UKF_CALIB_ERR="ctq:0.05,cth:0.05,m:0.05"` (현실적 캘리브 오차) → 기동(토크변동)마다 예측오차 →
  **gyro innovation 자연 상승 = 기동 aliasing**. "실기 캘리브 오차 반영" 프레이밍 = 비인위적. CLAUDE.md 로드맵 계획 축.
- **목표 프레임워크**: gyro 중간 aliasing → 슬라이딩 윈도우가 잡음 → 정탐/오탐 높게 · RHUKF > Adam.
- **검증 중**: clean(§6-2) vs cal-error 5% 대조 (results_62_* vs results_calib5_*).

---

## 6. 남은 것

```
⬜ cal-error 량 확정 — 기동 gyro aliasing 이 "중간"(윈도우가 잡는) 되도록 %  조절 (5% 시작)
⬜ drag x 확정 — 무풍일 고속 재측정
⬜ 실기 재비행 — 클램프 사라졌는지, 기동 NIS 기준선(F5~F8), gyro σ 방법통일
✅ 센서 σ 주입 (2026-08-20)
✅ Q·R — 노이즈 입력서도 R0.2/Q5e-3 최적 불변 (재검증)
```

## 백업
```
calibration.json.bak_pre_drag_0812 / .bak_pre_coupling_ctz_0812
run_sim.py / online_rl_main.py / swrl_config.py / env/ukf_filter.py — etc/backups/*.bak_pre_sensornoise_0820 등
```
