# sim 정합 변경사항 총정리 (2026-08-12)

> **이 문서가 현행이다.** `SIM_ALIGN_CHANGES_20260811.md` 는 이 문서로 대체됨(이력 보존용).

실기(Q250) ↔ Isaac SITL 플랜트 ↔ UKF 모델 3자 정합.
08-12 실기 주행(F2~F11, 11소티)으로 **yaw · 추력-토크 결합 · drag** 를 실측해 반영했다.

## 요약 — 3자 현재 상태

```
             ① 실기 Q250              ② Isaac 플랜트              ③ UKF (calibration.json)
질량          1.340 kg                1.340                      1.340                     ✅
호버 u        0.33                    0.326 (THR_MDL_FAC 0.78)   C_thrust 40.3             ✅
G roll        192.5                   physical 192.5             C_torque_x 3.261 → 193    ✅
G pitch       155~164                 155                        C_torque_y 3.10  → 155    ✅
G yaw         37.3                    ROTOR_CM 7.2e-7 → 1.195    C_torque_z 1.378 → 37.3   ✅ (−13%)
회전지연 τ     23ms (a2 .62~.69)       MOTOR_TAU 0.014            (UKF엔 지연 없음)          ✅
drag          x .352 / y .397         DRAG_XYZ 0.35,0.40,0       drag [0.35,0.40,0]        ⬜ x미확정
결합 K        [+.0018,+.0121,-.0020]  COM_ALIGN → 트림 부호일치    torque_thrust_coupling    ✅ (26%)
평시 NIS      gyro .60~1.17 vel .18~.41   gyro 0.84  vel 0.14      (동일 Q·R 로 재구동)      ✅ 범위 안
센서 σ        gyro [.066,.070,.044]   ⬜ 주입 미구현               (R 은 설계값, 정합 대상 아님)
              GPS vel [.088,.077,.243]
```

---

## 표 1 — 08-12 신규 반영

| 항목 | 파일 | old → new | 근거 | 상태 |
|------|------|-----------|------|------|
| **C_torque_z** | `calibration.json` | 4.41 → **1.378** | 실기 오토튠 log22 물리게인 Σb/dt 37.3 × Izz | ⚠ 잠정 ±20% |
| **torque_thrust_coupling** | `calibration.json` | [-.0012,+.0062,+.0001] → **[+.00182,+.01210,-.00195]** | 실기 11소티 호버 트림 (표준편차 14%) | ✅ NIS 로 검증 |
| **drag** | `calibration.json` | [0.50,0.30,0] → **[0.35,0.40,0]** | 실기 F4 등속법 x .352 / y .397 | ⚠ x 미확정 |
| **yaw 권한** | `run_sim.py` `rolling_moment_coefficient` | 1e-6 → **7.2e-7** | sim 실측 1.92 → 목표 1.378 (env `ROTOR_CM`) | ✅ 1.195 (−13%) |
| **plant drag** | `run_sim.py` `LinearDrag` | [0.50,0.30,0] → **[0.35,0.40,0]** | 실기 F4 등속법 (env `DRAG_XYZ`) | ✅ 주입 확인 |
| **바디 COM** | `run_sim._apply_com_align` | (신규) | K 를 기하로 재현 (좌표계·질량희석 보정 포함) | ✅ 롤·피치 부호 일치 |

### ⚠⚠ 함정 — `iris.py` 는 **죽은 코드**였다 (2026-08-12 발견)
`run_sim.py` 는 `IrisConfig` 가 아니라 **`MultirotorConfig()` 를 직접** 만든다(`run_sim.py:227`).
그래서 `iris.py` 를 고쳐도 플랜트에 **전혀 반영되지 않는다**. 기존 `ROTOR_K` 편집도 마찬가지로
계속 죽어 있었고, 플랜트는 내내 Pegasus 기본값(`rotor_constant 8.54858e-6`, `c_m 1e-6`,
`drag [0.50,0.30,0]`)으로 돌고 있었다.
→ 로터/항력 설정은 **`run_sim.py` 의 `config_multirotor` 블록**이 유일한 진짜 지점이다.
   `[ROTOR]` 로그로 매 기동 확인할 것. (구 문서의 "sim 로터 설정 = iris.py" 서술은 **오류**)

### 왜 COM 까지 건드리나 — 이게 이번의 핵심
`torque_thrust_coupling`(K) 은 **기하 상수**다. UKF 에 실기 K 를 넣었으면 sim 플랜트의 기하도
같아야 한다. 안 그러면 sim 에서만 보정이 틀려 평시 NIS 가 **반대로 올라간다**.
`calibration.json` 은 sim UKF 와 실기 UKF 가 **공유**하므로 이 정합이 없으면 한쪽이 반드시 깨진다.

```
τ = r̄ × F,  F=(0,0,−T),  r̄ = 로터중심 − COM
   τ_x = −T·ȳ  →  K_roll  = −ȳ
   τ_y = +T·x̄  →  K_pitch = +x̄
∴ COM = 로터중심 − (K_pitch, −K_roll, 0)
```

---

## 표 2 — 검증 결과 (실기 로그로 shadow UKF 재구동)

도구: `px4_field/nis_from_ulog.py` (신규) — sim 과 **같은 Q·R·같은 압축**으로 실기 NIS 재계산.

**평시 NIS 바닥이 5~10배 내려갔다. Q·R 은 한 글자도 안 건드렸다.**

| 비행 | vel NIS p50 | gyro NIS p50 | 압축 gyro p50 |
|------|-------------|--------------|---------------|
| f11_hover | 4.28 → **0.18** | 6.08 → **0.60** | 0.662 → **0.321** |
| f7_waypoint | 1.69 → **0.38** | 5.03 → **1.14** | 0.643 → **0.432** |
| f8_circle | 5.02 → **0.29** | 6.45 → **0.67** | 0.668 → **0.339** |
| f10_aggressive | 2.43 → **0.41** | 4.86 → **1.17** | 0.639 → **0.437** |

축별 자이로 잔차 RMS [rad/s] — 원인이 정확히 맞아떨어진다:

| 비행 | roll 전→후 | pitch 전→후 | yaw 전→후 |
|------|-----------|------------|----------|
| f11_hover | 0.77 → 0.52 | 2.66 → **0.95** | 1.43 → **0.34** |
| f8_circle | 1.20 → 0.61 | 2.22 → **1.30** | 2.06 → **0.39** |
| f10_aggressive | 1.56 → 0.87 | 2.42 → **1.78** | 0.90 → **0.63** |
| | ↑ K_roll | ↑ K_pitch (−64%) | ↑ C_torque_z (−76%) |

부수 효과: 압축기 여유 회복. 평시 압축값이 0.64~0.68 → 0.32~0.44 로 내려가
위쪽 여유가 0.25 → 0.5+ 가 됐다 (공격 신호가 압축에서 깎이던 문제 완화).

⚠ 함정 기록: K 를 트림 그대로 넣었더니(부호 반대) gyro NIS 가 **4배로 뛰었다**.
  K 는 **기하 토크**이고 트림은 그것을 상쇄하는 반대부호 명령이다 → `K = −τ_trim·C/T`.

---

## 표 3 — 08-12 실기 실측치 (근거 데이터)

| 물리량 | 값 | 출처 | 비고 |
|--------|-----|------|------|
| G 물리 Σb/dt | roll **192.5** / pitch **164.0** / yaw **37.3** | 오토튠 log22 (2회 COMPLETE) | roll 08-06 대비 −0.3% |
| a2 (모터지연) | .688 / .677 / .623 | 동일 | τ≈23ms 재확인 |
| 호버 트림 τ_norm | [−0.0073, −0.0554, +0.0042] | 11소티 중앙 | 표준편차 14%(pitch) |
| drag | x .352 (등속) / .176 (가속) · y .397 / .389 | F4 log28 | x 미확정, R² .12~.21 |
| C_thrust | 40.170 | F4 log28 | 정합값 40.3 과 0.3% |
| 자이로 σ | [.066, .070, .044] rad/s | f11_hover log33 | raw 기준 |
| GPS 속도 σ | [.088, .077, .243] m/s | 동일 | 호버=참속도 0 |
| GPS 레이트 | **10 Hz** | `vehicle_gps_position` 100ms | sensor_gps 1Hz 는 로거 스로틀 |
| 풍속(기울기 역산) | 0.3~1.6 m/s | 호버 기울기 | 체감보다 약했음 |

---

## 남은 것

```
✅ [②검증] sim 재비행 3회로 완료 — 표 4 참조. 세 항목 모두 통과.
✅ 속도 클램핑 해소 — flight_radius 3.5→2.8, agg_radius 2.8→2.24 (sim·실기 동시)
     전 패턴 v_max ≤ 1.40 m/s, MPC_XY_VEL_MAX 2.0 대비 여유 30%+
     ⬜ 다음 실기 비행에서 클램프가 사라졌는지 재확인 (안 사라지면 MPC_XY_VEL_ALL=-1 + VEL_MAX 2.5)
⬜ 센서 노이즈 σ 주입 (sim 측정에) — NIS 바닥의 현실성
⬜ drag x 확정 — 무풍일 고속 재측정 (MPC_XY_VEL_MAX 임시 3.0, --v 2.5 --leg 4 --reps 3)
⬜ Q·R 재튜닝 — ★ 위가 다 끝난 뒤. 공격 데이터 필요(F12 는 ATK_EN=0 이라 미주입)
```

---

## 표 4 — ★ sim 검증 결과 (3회 반복, `run_align_verify.sh` + `verify_align_0812.py`)

**세 항목 모두 통과. 정합 확정.**

| 항목 | sim 실측 | 실기 목표 | 판정 |
|------|---------|----------|------|
| ① 호버 트림 τ_norm roll | **−0.00540** | −0.00731 | ✅ 부호 일치, 26% |
| ① 호버 트림 τ_norm pitch | **−0.04085** | −0.05539 | ✅ 부호 일치, 26% |
| ① 호버 트림 τ_norm yaw | −0.00098 | +0.00417 | ⚠ 부호 반대 (아래) |
| ② C_torque_z | **1.195** | 1.378 | ✅ −13% (목표 자체가 ±20% 잠정) |
| ③ 평시 NIS vel p50 | **0.14** | 0.18~0.41 | ✅ |
| ③ 평시 NIS gyro p50 | **0.84** | 0.60~1.17 | ✅ 범위 안 |

### NIS 바닥이 수렴한 과정 (같은 Q·R, 물리만 바꿔가며)
```
              vel p50   gyro p50      무엇이 바뀌었나
1차 · 2차       0.44      2.72       COM 이 USD 에만 적용 + iris.py 죽은 코드 → 사실상 미반영
3차            0.14      0.84       run_sim 에 로터/항력 주입 + COM 좌표계·질량희석 보정
실기            0.18~0.41 0.60~1.17
```
**gyro 가 3.2배 내려가 실기 범위 안에 들어왔다.** sim 에서 뽑은 밴드·임계가 실기로 전이될 근거.

### 3차에서 잡은 세 가지 (1·2차가 왜 안 움직였나)
1. **`iris.py` 죽은 코드** — 위 ⚠⚠ 참조. `ROTOR_CM`/`DRAG_XYZ` 가 플랜트에 안 닿았다.
2. **COM y 부호 (좌표계)** — 로터중심은 월드 ENU(=스폰 자세 바디 **FLU**), 유도는 **FRD**.
   `FRD y = −FLU y` 라 y 항 부호가 뒤집힌다. → `COM_FLU = (c_x − K_pitch, c_y − K_roll, c_z)`
3. **로터 질량 희석** — 로터 4개가 별개 강체라 `r̄_system = (m_body/m_tot)·r̄_body ≈ 0.911·r̄_body`.
   목표를 `mass_frac` 으로 나눠 과보정한다.
   (부수: COM 은 USD 어트리뷰트만으로 안 먹는다 → `body_view.set_coms()` 필수, readback 확인)

### 남은 yaw 트림 불일치 (수용)
sim −0.00098 vs 실기 +0.00417. **COM 으로는 못 고친다** — 요 트림은 추력 오프셋이 아니라
로터 항력 비대칭에서 나온다. 크기가 롤/피치보다 한 자릿수 작고(피치의 2%),
gyro NIS 가 이미 실기 범위에 들어왔으므로 현 단계에서는 수용한다.

### 백업
```
calibration.json.bak_pre_drag_0812          (drag 반영 전)
calibration.json.bak_pre_coupling_ctz_0812  (K·C_torque_z 반영 전)
```
