# 실기 데이터 수집 현장 가이드 — 1차 (식별용)

> 목표: **UKF 모델 계수 + 센서 노이즈 바닥**을 실기체에서 얻는다.
> **컴패니언 컴퓨터 불필요. RC + Mission + ulog 만으로 전부 얻는다.**
> NIS 분포 대조용 B군 패턴 비행은 오프보드가 필요하므로 **2차로 미룬다**.
>
> 배경·계산식은 `SIM2REAL_PLAN.md`, 수집 스펙 전반은 `SIM2REAL_DATA_SPEC.md`.

---

## 왜 이것만으로 되는가 (1분 요약)

UKF 모델은 `C_torque` 와 `I` 를 **개별로 쓰지 않는다**. `_f()` 를 보면
`u[1]/I[0] = cmd_torque × (C_torque_x / Ixx)` — **비율만** 나온다.
그래서 암 길이·프로펠러·모터 사양이 **필요 없다**. 필요한 건:

| 얻을 것 | 어떻게 | 어느 비행 |
|---|---|---|
| `C_thrust = m·g / u_hover` | 질량 + 호버 스로틀 | F1 |
| `G_i = C_torque_i / I_i` | ω̇ vs 정규화 토크명령 | F2 |
| 결합항 `K` | 호버 트림 토크 ÷ 호버 추력 | F1 |
| `drag / m` | 등속 구간의 tilt vs 속도 | F3 |
| 자이로 노이즈 σ, GPS 속도 노이즈 σ | 정지 호버 (참속도≈0) | F1 |

**ESC 텔레메트리도 RTK도 필요 없다.** (텔레메트리는 `C_torque` 와 `I` 를 분리할 때만 필요했다.)

---

## 0. 전날 밤 — 벤치에서 (30분)

### 0-1. PX4 로깅 파라미터 (QGC)

| 파라미터 | 값 | 이유 |
|---|---|---|
| `SDLOG_PROFILE` | **High rate** 체크 (가능하면 "System identification" 도) | 기본 프로파일은 IMU 를 데시메이트해 진동 분석 불가 |
| `SDLOG_MODE` | **부팅 시부터 기록** | 지상 정지 구간이 있어야 센서 기준선을 잡는다 |
| `MPC_XY_CRUISE` | **3.0** (F3 용) | 미션 순항 속도 고정 |

> ⚠ 파라미터명·비트 의미는 PX4 버전마다 다르다. QGC 의 체크박스 라벨을 보고 고를 것.
> `SDLOG_PROFILE` 은 비트마스크라 숫자를 직접 넣지 말고 QGC UI 로 체크하는 편이 안전하다.

### 0-2. 로그에 필요한 메시지가 실제로 찍히는지 확인

**시동 없이 지상에서 30초 로깅 → SD 카드 뽑아 확인.**
```bash
ulog_info <파일>.ulg | head -40      # 메시지 목록·개수
python3 check_ulog.py <파일>.ulg --type ground
```
아래가 **전부** 있어야 한다. 하나라도 없으면 현장에서 헛수고가 된다.
```
vehicle_thrust_setpoint     ← C_thrust
vehicle_torque_setpoint     ← G, 결합항 K
sensor_combined             ← 자이로/가속도 원시, 진동 PSD
vehicle_angular_velocity    ← ω (필터링본)
vehicle_attitude            ← 자세
vehicle_local_position      ← 속도 (drag)
sensor_gps (또는 vehicle_gps_position)  ← GPS 속도 노이즈
battery_status              ← 전압 드리프트
vehicle_status              ← 모드/시동 상태
```

### 0-3. 기체 상태
- 프로펠러 밸런싱·손상 확인 (진동은 자이로 채널 = 주 탐지 feature 에 직결)
- 배터리 만충. **비행마다 같은 충전 상태에서 시작**

---

## 1. 현장 도착 후 — 수기 기록 (5분)

| 항목 | 비고 |
|---|---|
| **AUW (실제 이륙 중량)** | ★ 필수. 배터리·짐벌 포함 **날리는 그대로** 저울에 |
| 프로펠러 각인 | 예 `9450` — 30초면 읽는다. 없어도 되지만 있으면 교차검증에 쓴다 |
| 배터리 셀 수·용량 | 라벨 확인 |
| 풍속 체감 / 기온 | 대략이면 충분 |
| 비행별 로그 파일명 + 시작시각 + 조건 | **이게 제일 중요하다.** 나중에 어느 로그가 뭔지 못 찾으면 다 헛수고 |

### 조건
- **바람 약한 때** (F1 은 특히). 나뭇잎이 흔들리면 F1 은 다시
- **고도 3 m 이상** (지면효과 회피). F2 는 **10 m 이상** 여유
- 넓은 공터, 안전조종사 확보

---

## 2. 비행 목록 (배터리 배분)

| 배터리 | 비행 | 모드 | 소요 |
|---|---|---|---|
| **#1** | F1 호버 60s → F2 자세여기 | Position → Stabilized/Acro (또는 오토튠) | ~6분 |
| **#2** | F3 직선왕복 (전후·좌우) | **Mission** | ~7분 |
| **#3** | F4 수직왕복 + F1 재촬영(예비) | Position | ~6분 |

여유 있으면 **F1 을 두 번** 뜨는 걸 권한다. 가장 값어치가 크고 가장 짧다.

---

## 3. 비행별 상세

### F1. 정지 호버 60초 ★ 가성비 최고
**모드**: Position (스틱 중립)

```
1. 이륙 → 고도 4~5 m
2. 자세 안정될 때까지 10초 대기
3. 스틱에서 손 떼고 60초 이상 그대로 둔다
4. 로그 시각 기록
```

**얻는 것 4가지**
- `C_thrust = m·g / mean(|vehicle_thrust_setpoint.xyz[2]|)`
- 결합항 `K[i] = mean(vehicle_torque_setpoint.xyz[i]) / mean(|thrust_sp[2]|)` — 호버 트림이 곧 COM 오프셋
- 자이로 노이즈 σ = `std(sensor_combined.gyro_rad)`
- **GPS 속도 노이즈 σ** = `std(sensor_gps.vel_*)` — 정지 호버는 참속도 ≈ 0 이므로 **GPS 속도가 곧 노이즈다**

**품질 기준**: 수평속도 |v| < 0.3 m/s 가 60초 연속. 바람에 밀리면 다시.

---

### F2. 자세 여기 ★ 폐루프 편향을 피하는 유일한 비행
**목적**: `G_x, G_y, G_z = C_torque / I` 식별

> ⚠ **평범한 Position 모드 비행 로그로 회귀하면 안 된다.** 자세 rate loop 는 폐루프라
> `ω̇ ~ τ_cmd` 회귀가 편향된다 — sim 에서 **14배 틀린 값**(0.265 vs 3.57)이 나온 실증이 있다.
> 명령이 상태와 무관하게 흔들려야 한다.

**옵션 A — PX4 자동 튜닝 (권장, 가장 쉽고 안전)**
```
QGC → Vehicle Setup → PID Tuning → Autotune
고도 10 m 이상, 넓은 공터, 자동 실행 후 착륙
```
자동으로 여기 신호를 주입해 rate loop 를 동정한다. 목적이 정확히 일치하고, 그 로그를 그대로 쓴다.
(파라미터명·메뉴 위치는 버전 확인)

**옵션 B — 수동 doublet (오토튠이 없거나 실패하면)**
```
모드: Stabilized (익숙하면 Acro)
고도 10 m 이상

롤:   스틱 오른쪽 톡 → 왼쪽 톡 → 중립     (각 0.3~0.5초, 사이 1초 쉼)  × 8회
피치: 앞 톡 → 뒤 톡 → 중립                                        × 8회
요:   오른쪽 톡 → 왼쪽 톡 → 중립                                   × 8회
```
- **짧고 날카롭게.** 길게 밀면 폐루프가 따라잡아 여기가 죽는다
- 축을 **섞지 말 것** — 롤 8회 끝내고 피치 8회. 섞으면 축 분리가 안 된다
  (sim 에서 설계행렬 조건수 1412 가 나와 롤/피치 분리에 실패한 전례가 있다)
- 자세가 30° 넘게 기울면 과하다. 10~20° 정도의 응답이면 충분

---

### F3. 직선 왕복 — drag
**모드**: **Mission** (수동보다 훨씬 정확하다. 등속을 손으로 못 잡는다)

```
미션 구성:
  전후 방향  A → B → A → B   (직선 구간 40 m 이상, 고도 5 m 고정)
  좌우 방향  C → D → C → D   (전후와 직각)
MPC_XY_CRUISE = 3.0 m/s
```
- 각 구간에서 **등속 구간이 3초 이상** 나와야 한다. 항력은 등속에서만 보인다
- 여유 되면 크루즈 속도를 **2 / 4 / 6 m/s** 로 바꿔 3세트 → drag 가 속도에 선형인지 확인 가능
- 요는 진행방향으로 두든 고정하든 상관없다 (로그에 자세가 있으므로)

**얻는 것**: `drag_x`, `drag_y` (등속에서 추력 수평성분 = 항력)

---

### F4. 수직 왕복 (선택)
**모드**: Position, 고도 3 m ↔ 10 m 를 ±2 m/s 로 4~5회 왕복
**얻는 것**: `drag_z` (sim 은 0.0 — 확인용). 시간 남으면.

---

## 4. ★ 비행 직후 현장 검증 — 짐 싸기 전에

**집에 와서 "데이터가 못 쓰겠다"를 발견하면 하루가 날아간다.** 현장에서 확인한다.

```bash
python3 check_ulog.py <파일>.ulg --type hover      # F1
python3 check_ulog.py <파일>.ulg --type doublet    # F2
python3 check_ulog.py <파일>.ulg --type drag       # F3
```

각 검사가 **PASS** 를 내야 한다. FAIL 이면 **그 자리에서 다시 뜬다.**
스크립트는 필요한 메시지 유무·레이트·품질 게이트를 보고, 가능하면
`C_thrust`·트림·노이즈 **예비 추정치까지 즉석에서 뽑아준다** (값이 말이 되는지 눈으로 확인).

노트북에 미리 준비:
```bash
pip install pyulog        # 이미 설치돼 있으면 생략
# check_ulog.py 를 USB 나 노트북에 복사해 갈 것
```

---

## 5. ROS2 토픽 (이번엔 **안 써도 된다**)

ulog 가 레이트·정보량 모두 우수하고 링크 문제도 없다. **1차는 ulog 만으로 충분하다.**

다만 물어보셨으니 — 나중에 컴패니언 컴퓨터로 오프보드를 붙일 때 필요한 토픽은:

```
# 구독 (PX4 → 컴패니언)
/fmu/out/sensor_combined              ← 자이로 (UKF 관측 z[6:9])
/fmu/out/sensor_gps                   ← GPS 위치·속도 (UKF 관측 z[0:6])
/fmu/out/vehicle_thrust_setpoint      ← u[0]
/fmu/out/vehicle_torque_setpoint      ← u[1:4]
/fmu/out/vehicle_attitude             ← 자세 (★ 아래 주의)
/fmu/out/vehicle_local_position       ← 위치/속도
/fmu/out/vehicle_status               ← 모드/시동

# 발행 (컴패니언 → PX4)
/fmu/in/trajectory_setpoint           ← 궤적 or 호버 latch
/fmu/in/offboard_control_mode         ← ≥2 Hz 로 계속 (끊기면 페일세이프)
/fmu/in/vehicle_command
```

> ⚠ **`vehicle_attitude` 프레임 함정**: PX4 의 자세 쿼터니언은 **이미 NED/FRD** 다.
> `online_rl_main._quat_to_euler()` 는 Isaac 의 ENU/FLU 를 NED 로 **변환**하는 함수라,
> 실기 쿼터니언에 그대로 적용하면 2026-07-28 에 고친 프레임 버그가 되살아난다.
> 증상이 "기동 중 vel NIS 오염" 이라 조용히 망한다.

> ⚠ uXRCE-DDS 는 `dds_topics.yaml` 에 등재된 토픽만 내보낸다.
> `esc_status`, `actuator_motors`, `vehicle_imu_status` 는 기본에 없을 수 있으니 미리 추가·빌드.

---

## 6. 가져올 것 체크리스트

```
□ 기체 + 프로펠러 예비
□ 배터리 3개 이상 (만충)
□ 저울  ← AUW 측정용. 잊기 쉽다
□ 노트북 + check_ulog.py + pyulog 설치됨
□ SD 카드 리더 (또는 QGC 로그 다운로드 확인)
□ QGC 깔린 노트북/태블릿 (파라미터·미션·오토튠)
□ 조종기 (안전 오버라이드용, 모든 비행에 필수)
□ 수기 기록용 종이/폰 메모  ← 로그 파일명 ↔ 비행 매칭
```

---

## 7. 우선순위 (시간·배터리가 모자랄 때)

```
1순위  F1 호버 60초     ← 이거 하나가 4가지를 준다. 절대 빼지 말 것
2순위  F2 자세 여기      ← G 없으면 모델이 안 선다
3순위  F3 직선 왕복      ← drag. 없으면 sim 값(0.5/0.3) 임시 사용 가능
4순위  F4 수직 왕복      ← 있으면 좋음
```

**F1 + F2 만 성공해도 1차 목적은 달성이다.**

---

## 8. 돌아온 뒤

```bash
python3 check_ulog.py <각 로그> --type <유형>     # 다시 한 번 정밀 검사
# → 이후 계수 추출 스크립트로 C_thrust / G / drag / K 산출 (제작 예정)
# → sim 플랜트 관성 스케일 (I_sim = C_torque_iris / G_real)
# → P1 기준선 → P2 밴드(rad/s² 단위) → 관측 정규화 결정 → 학습
```
