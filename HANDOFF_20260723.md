# 프로젝트 핸드오프 — UAV 액추에이터 공격 탐지 RL (2026-07-23)

> 새 대화용 자기완결 브리핑. 이 문서 하나로 현재 상태·직전 결정·다음 할 일을 파악할 수 있게 정리함.
> (원 컨텍스트: Claude Code + 리포 `Issacsim-rhukf`, CLAUDE.md·메모리에 상세 있음)

---

## 1. 한 줄 요약
쿼드로터 **제어입력(액추에이터) 공격 탐지**를 POMDP/QCD stopping 문제로 정식화. UKF 잔차(NIS) 관측 기반
**DDQN**이 매 스텝 track/hover 이진 결정. **최종 기여 = Adam 대비 커스텀 2차 최적화기 RHUKF-FV(FIR 철학)의
sparse-attack 샘플효율 우위.** 센서 공격은 스코프 밖(센서 무결성 가정).

## 2. 시스템 스택
- Isaac Sim 4.5 + PX4 SITL + Pegasus, ROS2 Humble, **10Hz 제어루프**, sim_speed_factor=10.
- `online_rl_main.py`(RL/스윕 노드) + `run_sim.py`(Isaac 서브프로세스).
- 선행연구 대비: EADR(고정 CUSUM, 시나리오별 하드코딩 임계값 — 저자들이 future work으로 RL 지목),
  QUADFormer(센서공격+transformer, TTD 미보고).

## 3. 문제 정식화
- **관측**: 4스텝 윈도우 `[nis_vel, nis_gyro, action]` = **12차원**. 의도적 비일관 UKF(Q낮게/R높게)
  → NIS는 χ² 통계량이 아니라 **탐지 feature**.
- **NIS 압축(통일, 2026-07-22)**: 전 채널 `ε̃ = ln(1+ε)/(1+ln(1+ε))` [offset=1.0]. (기존 vel=log0.5 폐기.)
  `env/ukf_filter.py:compute_nis_scaled`. 학습·스윕 경로 동일 함수.
- **pos NIS**(res[0:3], R_pos=0.5)는 **로깅 전용**(정책 입력 아님). 16D(pos 포함) 채택은 분리도로 판정(아래 STEP 3).
- **행동**: 이진 track/hover. stopping-time POMDP 표준(복귀 학습=양방향 stopping으로 QCD 확장).
- **네트워크**: 페어링용 `[24,24]` = 962params@12D / 1058@16D. 최적화기 공정비교용 고정. `--agent rhukf|adam`.

## 4. 공격 세팅 (동결 상태 + 직전 발견)
- **채널 = COMBINED**, `bias_ft_ratio=1.5` (torque:thrust=1:1.5, T≈2N@s=1.34), `bias_yaw_ratio=0.2`, **ramp=0.0**.
  튜브 샘플러: (torque_xy=s, torque_z=0.2·s, thrust=1.5·s), 성분별 ±10% jitter.
- **밴드(구): [1.34, 1.40] Nm** = "track추락 ∧ hover생존" — **단, aggressive 패턴에서만 검증됨**.
- 공격 시간구조: single 모드, 온셋 U(50,150), 지속 U(50,100). (burst 모드도 구현되어 있으나 현재 single.)
- `attack_types=['loe_combined']`, `prob_no_attack=0.15`.

## 5. ★ 직전 결정 (2026-07-23) — 이게 지금 작업의 출발점 ★
### 5-1. 버그 수정: 전 패턴 공격
- **`attack_flight_patterns=['aggressive']` → flight_patterns 전체**(waypoint/circle/figure8/aggressive).
- 실제 공격은 **모든 기동 궤적에 랜덤 세기로** 주입 = 의도된 설계(사용자 확정). 기존 aggressive 강제는 버그.
- swrl_config.py:117 + sampler:388 수정 완료. 검증: 공격에피 4패턴 균등(~830/4000ep).

### 5-2. 발견: 밴드 [1.34,1.40]은 aggressive 전용 (전 패턴 미성립)
`results_deadline` (hover/waypoint/figure8 × bias{1.34,1.37,1.40} × {track,dhover0/3/5/7} × 12ep) 분석:

| 패턴 | track 생존(①결과성=낮아야) | dhover0=hover(③대응=높아야) | 판정 |
|---|---|---|---|
| figure8 | 1.00 (전 bias) | 1.00 | ① **붕괴** — 공격 무해, onset>1.40 |
| hover* | 1.00 (전 bias) | 1.40서 0.00 | ① 붕괴 + 호버전환 사망(soft-hold 이슈) |
| waypoint | 1.37서 0.25 | 1.37서 1.00 | 1.37만 부분성립(dh3=0.17=데드라인 빡빡) |

- **aggressive = 최악(가장 취약) 케이스.** 순한 패턴은 제어여유가 커서 같은 bias를 흡수 → 밴드가 **위로 이동/무해**.
- (*hover는 flight_patterns에 없음 = 공격패턴 아님. 대응정책/캡처용 패턴.)
- **함의**: [1.34,1.40] 균일 주입 시 figure8/circle 공격이 **비결과성** → "공격분포를 결과성 밴드로 한정해
  탐지=위협이 설계상 참" 프레이밍 보증이 깨짐. → **패턴별 밴드 재보정 필요**(다음 로드맵).

---

## 6. 로드맵 (합의됨, 상세는 `ROADMAP_bandsweep_20260723.md`)

### 결정된 원칙
- **패턴별 결과성 밴드 재스윕** (공격은 전 패턴 유지).
- **밴드 폭 = 물리범위 `[onset, hover한계]`를 패턴별로 꽉 채워 넓게.**
  근거: 밴드 안이면 전 구간 ①∧③ 성립 → 넓혀도 탐지=위협 안 깨짐(하한 아래만 금지). severity 다양성 =
  robustness + 현실성 + 샘플효율 서사. 하한(near-threshold)이 RHUKF-FV vs Adam 판별구간.
- **스윕 외란 = 바람 0** (`disturbance=none`, 결과성 격리). combined ft1.5, ramp0.0, 온셋 고정@step100.

### STEP 1 — 패턴별 밴드 스윕
- 패턴: waypoint, circle, figure8 (+ aggressive 대조). 정책: track(①) + dhover{0,3,5}(③). ep/cell=20.
- 예상 그리드: waypoint 1.33~1.41 / circle 넓게 스캔 / **figure8 1.40~1.72(onset>1.40)** / aggressive 1.32~1.40.
- 1차 거친 스캔 → 2차 0.02 세밀화. CLI:
  ```
  python3 online_rl_main.py --headless --sweep --sweep-mode combined \
    --sweep-pattern figure8 --sweep-values 1.40,1.48,1.56,1.64,1.72 \
    --ramp 0.0 --sweep-wind-type none --sweep-wind-speed 0 --speed 10 --outdir results_band_figure8
  ```
- **판정**: 하한=track 생존율 유의하락 시작(≲0.5), 상한=dhover0 유지 마지막점, 밴드 내 dhover d≤3=hover(1.0).

### STEP 2 — 샘플러 코드 확장
- 단일 `bias_scale_range` → **패턴별 dict** (`bias_scale_range_by_pattern`). jitter/클립 로직 유지·패턴별 상한.

### STEP 3 — 관측 재산출 (P2)
- 압축 통일로 기존 d′/baseline 무효 → 재산출. **전 패턴 공격 vs 전 패턴 기동** 분리도: raw / 압축 / +pos NIS(12D vs 16D).
- 침묵구간 분리도 개선폭 = 16D 채택 여부. 바람(none40/turb60, 1–5) 이 단계에서 복귀.

### STEP 4 — 동결 & 본실험
- 환경·관측 동결 → 본 페어링 **seed 3개**(Adam γ0.85 + RHUKF 동일 config). 공격밀도(burst 수) 축 학습곡선.
  RHUKF-FV vs Adam vs CUSUM.

---

## 7. 핵심 물리 지식 (스윕 실측 확정)
1. s<1.2 공격은 PX4가 중화 → 공격 정의에서 배제.
2. **step-류 공격의 잔차 시그니처 = 펄스**: t=1~2 스파이크 → t=3~8 침묵(PX4 보상) → 후기 재상승.
   CUSUM은 펄스 누적 불가(지연 7~15스텝), **윈도우 패턴매칭(RL)은 t=1~2 포착 가능 = RL 정당화 근거.**
3. 호버-공격 평형은 흡인영역(basin)이 좁음: 처음부터 호버면 생존, 기동+온셋 노출 후 전환하면 서서히 발산.
   전환 과도 자체는 무해(고도캡처 수정 후).
4. combined 죽음의 주 경로 = 추력 채널(crash_altitude), 호버로 흡수 불가.
5. ramp 0.1s(=1스텝@10Hz)는 사실상 step.

## 8. 프레이밍 결정 (논문 서사 — 바꾸지 말 것)
- **탐지 프레이밍 유지**(위협평가 프레이밍 기각): 공격분포를 결과성 밴드로 한정해 "탐지=위협"이 설계상 참.
- **호버 = 공격불가지론 fallback + 검증기동(능동센싱). "recovery"라 부르지 않음.**
  - ⚠ 사용자 제기: 호버는 자세안정 측면도 있음(setpoint→roll/pitch/rate 0, 제어적 사실). 단 "recovery" 프레이밍과
    충돌 소지 → **현재는 메커니즘 관찰로 유지(왜 조건③이 패턴별로 다른가의 설명), 서사 승격 보류.**
- 이진 행동 = stopping-time POMDP 표준. ramp = 실험완화 아니라 "탐지-대응 유의미한 공격 클래스"의 정의.
- gamma 낮춤으로 즉각성 확보 안 함(즉각성=per-step 페널티, gamma=시야).
- **급기동/외란 = aliasing 원천 = RL 존재 이유. 최종 평가에서 제거 금지(ablation 축으로만).**

## 9. 프레임워크 성립 3조건 (동시 필요)
① **결과성**: 무대응(track) 시 추락  ② **탐지 가능성**: NIS 온셋 엣지 존재
③ **대응 가능성**: 탐지지연 d≤3스텝 전환으로 생존(dhover 생존율 ≈ hover)
현재 과제 = 3조건이 겹치는 (공격채널, 패턴, bias 범위)를 **패턴별로** 확정.

## 10. 미해결 / 주의
- hover-패턴 1.40 dhover0 사망(soft-hold): `results_deadline_softhold` 비교 존재 → soft-hold 구제 여부 확인.
- per-pattern 밴드 heterogeneity → 로깅에 pattern별 bias_scale 유지.
- **`--sweep-values`는 쉼표 구분** (공백이면 뒤 값 조용히 버림 — 실사고 2회).
- 스윕 시작 30초 내 로그에서 ramp/bias목록/셀수 확인. 결과폴더 실험별 분리, 논문수치는 셀당 20+ep.
- Isaac 종료 시 SimManager가 run_sim 재기동 → 재시작 전 `pkill run_sim` 확인. speed10 실측 RTF ~2.6x.

## 11. 관련 파일
- `CLAUDE.md` — 전체 프로젝트 컨텍스트(상세). `ROADMAP_bandsweep_20260723.md` — 이번 밴드 스윕 상세 로드맵.
- `swrl_config.py` — config/샘플러. `online_rl_main.py` — RL·스윕 노드. `env/ukf_filter.py` — NIS 압축.
- `results_deadline/`, `results_deadline_softhold/` — 밴드 재보정의 근거 데이터.
- 집계: `python3 sweep_aggregate.py <dir>`.
