# 발표 정리 — 재캘리브레이션과 실기 데이터 수집

> 슬라이드 단위로 구성. 각 절이 슬라이드 1장 분량.

---

## 슬라이드 1. 지금 무엇을 하고 있는가

**프로젝트**: UAV 액추에이터 공격 탐지
- 공격이 들어오면 **UKF 예측과 실제 센서값이 어긋난다** → 그 잔차(NIS)를 관측으로
- RL 정책이 매 스텝 **track(임무 계속) / hover(안전 fallback)** 이진 결정
- 기여: Adam 대비 커스텀 2차 최적화기 **RHUKF-FV의 샘플효율 우위**
- 최종 목표: 학습된 정책을 **실기 PX4 드론에 올려 online inference**

**현재 단계**: 모델-플랜트 불일치 3건을 발견·수정 → **기존 측정치 전부 무효 → 재측정 중**

```
Phase 0 (완료)  플랜트 검증 · 모델오차 항 닫기
P1 평시 기준선 → P2 공격 밴드 → P3 데드라인 → P4 분리도 → P5 학습
     ↕ 병렬
실기 트랙  계수 식별(내일 비행) → 플랜트 정합 → 배포
```

---

## 슬라이드 2. 왜 캘리브레이션이 이 연구의 전제조건인가

정책이 먹는 것은 **NIS = 잔차의 정규화 크기**다.

```
NIS = rᵀ Pzz⁻¹ r / n     ,   r = z_측정 − z_모델예측
```

- NIS 는 **모델오차를 드러내도록** 설계된 통계량이다 (숨기는 게 아니다)
- 캘리브레이션이 틀리면 → 평시에도 잔차가 크다 → **NIS 바닥이 올라간다**
- 바닥이 올라가면 **공격 신호가 그 위에 묻힌다**

> **즉 캘리브레이션 오차 = 탐지 성능의 상한을 직접 깎는다.**
> 모델 정확도가 목적이 아니라, **탐지 신호 대 잡음비**가 목적이다.

---

## 슬라이드 3. UKF 모델 — 각 상수가 들어가는 자리

12상태 UKF: `x = [위치(3), 오일러각(3), 속도(3), 각속도(3)]`

```
위치     ṗ  = v
자세     Φ̇  = W(φ,θ) · ω                        ← 오일러 kinematics
속도     v̇  = R(φ,θ,ψ)·(F_thrust + F_drag)/m + g
각속도   ω̇  = I⁻¹ (τ − ω × Iω)
```

| 상수 | 들어가는 자리 | 물리적 의미 |
|---|---|---|
| **m** (질량) | `v̇ = F/m` | 힘 → 가속도 변환 |
| **I** (관성) | `ω̇ = I⁻¹τ` | 토크 → 각가속도 변환 |
| **C_thrust** | `F_thrust = C_thrust · cmd_thr` | 정규화 명령 → 물리 추력 [N] |
| **C_torque** | `τ = C_torque · cmd_tq` | 정규화 명령 → 물리 토크 [N·m] |
| **R(φ,θ,ψ)** | 추력벡터 회전 | 바디 → 월드 좌표 (**프레임 규약**) |
| **drag** | `F_drag = −drag ⊙ v_body` | 속도 비례 공기저항 |
| **K** (결합항) | `τ += K · F_thrust` | 로터가 COM 에서 어긋나 총추력이 만드는 상시 토크 |

**하나라도 틀리면 그 방정식이 통째로 어긋나고, 그 오차가 NIS 로 나온다.**

---

## 슬라이드 4. 수정 1 — 총 비행질량

| | 값 | 근거 |
|---|---|---|
| 전 | **1.6186 kg** | Iris USD: body 1.5 + **로터 4개 0.1186** |
| 후 | **1.372 kg** | 사용자 실기체 AUW 로 정합 |

**무엇이 문제였나**
- 문서·예제의 "1.5 kg" 은 **바디만**의 값
- Iris 의 로터 4개는 **각각 별개 강체**(리볼루트 조인트)라 질량이 따로 잡힌다
- 로터를 빼먹은 것이 오해의 출발점

**다이나믹스에서의 역할**: `v̇ = F/m` — 추력 → 가속도 변환 전체

**영향**: 추력대중량비 **T/W 2.61 → 3.07** (기동성이 15% 올라감)
→ 같은 세기의 공격이 더 큰 각가속도를 만든다 → **공격 밴드가 아래로 이동할 것으로 예상**

---

## 슬라이드 5. 수정 2 — 추력·토크 계수

| 계수 | 전 | 후 | 배율 |
|---|---|---|---|
| C_thrust | 22.82 | **25.58** | 1.12× |
| C_torque_xy | 0.265 | **x 3.568 / y 4.017** | **약 14×** |
| C_torque_z | 3.655 | **4.907** | 1.34× |

**C_torque 가 14배나 틀렸던 이유 — 폐루프 편향**
- 자세 rate loop 는 **폐루프**다. 컨트롤러가 오차를 이미 없앤 뒤의 데이터로
  `ω̇ ~ τ_cmd` 회귀를 돌리면 계수가 심하게 편향된다 (R² 0.03~0.15)
- 해법: 명령이 아니라 **실제 로터 각속도**로 적용 토크를 직접 계산

**증상이 명확했다**
```
구 모델 최대 롤 각가속도 예측   10 rad/s² 미만
실측 피크                      52 rad/s²      ← 원리적으로 설명 불가
신 모델 예측 상한             121 rad/s²      ← 실측을 포함 (실측은 상한의 43%)
```
> 기존에 "기동과 공격을 구분하기 어렵다(aliasing)"고 해석하던 현상의
> **상당 부분이 이 계수 오류**였다.

---

## 슬라이드 6. 수정 3 — 좌표 프레임 버그

**Isaac Sim 의 자세 쿼터니언은 ENU 관성 / FLU 바디 규약**
→ 표준 ZYX 오일러 공식을 적용한 각을 **NED 기준인 양 소비**하고 있었다

**피해 두 곳**

1. **UKF**: 추력벡터를 틀린 수평 방향으로 회전 → 기동 중 속도 잔차 오염
2. **제어**: 호버 전환마다 약 **90° 요 슬루**를 명령
   (실측 25회: 전환 후 3초 |Δψ| 중앙 **84.5°** vs 전환 전 0.7°)

**검증 — 항력이 리트머스 시험지**

Pegasus 소스에 `LinearDrag([0.50, 0.30, 0.0])` 이 하드코딩돼 있다(= 정답을 안다).

| | 항력 적합값 | 상관계수 |
|---|---|---|
| 프레임 틀렸을 때 | [0.13, 0.03] | −0.30 |
| **수정 후** | **[0.499, 0.299]** | **−1.00** |

→ 설정값을 정확히 복원. **프레임이 맞다는 독립 증거.**

---

## 슬라이드 7. ★ 수정 4 — 추력-토크 결합항 (최대 성과)

**물리**: Iris 는 로터 중심이 바디 COM 에서 **(x +6.7 mm, y −1.9 mm)** 어긋나 있다
→ **총추력이 상시 토크를 만든다** → PX4 가 상시 트림 토크로 상쇄

**문제**: UKF 모델에 이 항이 없으면 **그 트림 명령을 실제 토크로 오해**한다

```
호버에서 PX4 트림       u2_ty = −0.0858 N·m
모델의 잘못된 예측       ω̇y = −3.24 rad/s²        (실제로는 0)
자이로 칼만이득이 약함    |K| ≈ 0.041 → 상태가 안 당겨짐
정상상태 잔차            2.07 rad/s               (측정은 0.009)
```

**결과 — 평시 자이로 NIS 바닥**

| 조건 | nis_gyro | nis_vel |
|---|---|---|
| 결합항 없음 | 2.910 | 0.164 |
| **결합항 포함** | **0.006** | **0.031** |
| | **485배 감소** | 5.3배 |

> 이전 판단은 "이 항의 NIS 기여 ≈ 0.018 (무시 가능)" 이었다.
> **실제는 약 160배 큰 값**이었고, 자이로 채널 바닥의 **96%** 를 차지하고 있었다.
> (C_torque 를 14배 올리면서 숨어 있던 이 누락이 드러났다 — 두 오류가 맞물려 있었다)

---

## 슬라이드 8. 왜 이 차이가 탐지 성능을 좌우하는가

관측은 압축된다: `ε̃ = ln(1+ε) / (1 + ln(1+ε))`

**같은 세기의 공격이 바닥에 따라 이만큼 달라진다**

| 평시 바닥 | 평시 ε̃ | 공격 시 ε̃ | **대비(신호)** |
|---|---|---|---|
| 2.910 (수정 전) | 0.577 | 0.686 | **0.110** |
| **0.006 (수정 후)** | 0.006 | 0.642 | **0.636** |

> **동적 범위 5.8배 차이.** 바닥이 높으면 공격이 압축에 묻힌다.
> 캘리브레이션 수정이 곧 **탐지 신호 대 잡음비 5.8배 개선**이다.

---

## 슬라이드 9. 부수 결과 — 모터 지연은 기각

실기 ESC+모터 지연(τ_up 30 ms / τ_down 70 ms)을 sim 에 주입해 봤다.

| 항목 | τ=0 | τ 주입 | 배율 |
|---|---|---|---|
| \|gyro_x\| 중앙 | 0.011 | 1.069 | **100×** |
| \|gyro_z\| 중앙 | 0.038 | 2.157 | **57×** |
| nis_gyro 중앙 | 2.910 | 12.180 | 4.2× |

**원인**: PX4 게인이 **지연 없는 시뮬레이터 플랜트**에 튜닝돼 있어 자세 루프가 발진

**교훈 — 파라미터 단독 이식은 위험하다**
```
sim  : (τ=0,   게인 A)  ← 각각 내부적으로 일관
real : (τ>0,   게인 B)
τ 만 옮기면 → 어느 쪽도 아닌 제3의 불안정 플랜트
```
→ 맞춰야 할 것은 τ 가 아니라 **폐루프 자세 응답**. 현재는 τ=0 유지 + 차이를 명시.

---

## 슬라이드 10. 실기 비행 — 무엇을 얻어 무엇을 맞추나

### 핵심 통찰: 기체 스펙이 필요 없다

UKF 모델을 보면 `C_torque` 와 `I` 가 **개별로 등장하지 않는다**:
```
ω̇ = τ/I = (C_torque · cmd) / I = cmd × (C_torque / I)
                                        └──── 비율만 ────┘
```
→ **암 길이·프로펠러·모터 사양 불필요.** ESC 텔레메트리·RTK 도 불필요.
→ 필요한 것은 **질량 + 기본 비행 로그**뿐.

### 비행 3종

| 비행 | 얻는 원자료 | 맞추는 파라미터 |
|---|---|---|
| **F1 정지 호버 60초** | 호버 스로틀, 트림 토크, 자이로·GPS 잡음 | **C_thrust**, **결합항 K**, 센서 노이즈 σ |
| **F2 자세 여기**(오토튠/doublet) | ω̇ vs 정규화 토크명령 | **G = C_torque / I** |
| **F3 직선 왕복**(Mission) | 등속 구간의 자세 vs 속도 | **drag** |

**F1 이 가장 가성비가 높다** — 60초에 4가지가 나온다.
정지 호버는 **참 속도 ≈ 0** 이므로 GPS 속도가 곧 GPS 노이즈다 (RTK 불필요).

**F2 는 반드시 개루프 여기**여야 한다. 평범한 비행 로그로 회귀하면 폐루프 편향으로
**14배 틀린다** (슬라이드 5 에서 실증됨).

---

## 슬라이드 11. 실기 데이터를 받은 뒤의 흐름

```
1. 계수 추출        C_thrust, G, drag, K
2. sim 플랜트 정합   I_sim = C_torque_iris / G_real   ← 관성만 스케일
                    (로터 기하는 그대로, USD 수정 불필요)
3. P1 평시 기준선   → 실기와 sim 의 NIS 바닥 대조
4. P2 공격 밴드     → rad/s² 단위로 기록 (전이 가능한 단위)
5. 관측 정규화 결정  → 정책이 baseline 불변성을 학습
6. P5 학습          → 배포 후보 정책
7. 실기 배포        모니터 전용 → 폐루프 (단계적)
```

**밴드를 rad/s² 로 기록하는 이유**
- sim: 공격을 **물리 토크(N·m)** 로 바디에 직접 인가
- 실기: 공격을 **정규화 명령 bias** 로 주입
- 두 주입의 등가 조건 = **주입 각가속도** `α = τ/I = δ · G`
→ N·m 로 기록하면 전이 안 되고, rad/s² 로 기록하면 전이된다

---

## 슬라이드 12. 정리

**한 문장**: 모델-플랜트 불일치 4건을 닫아 **탐지 신호 대 잡음비를 5.8배 개선**했고,
이제 그 모델을 실기체에 정합시키는 단계다.

| 수정 | 전 → 후 | 다이나믹스 역할 |
|---|---|---|
| 질량 | 1.6186 → 1.372 kg | 힘 → 가속도 |
| C_thrust | 22.82 → 25.58 | 명령 → 추력 |
| C_torque | 0.265 → 3.57 / 4.02 (14×) | 명령 → 토크 → 각가속도 |
| 프레임 | ENU/FLU → NED/FRD | 추력벡터 회전 |
| **결합항 K** | **없음 → 포함** | **COM 오프셋 보정** |

**측정된 개선**
```
평시 자이로 NIS 바닥   2.910  →  0.006     (485배)
탐지 신호 대비        0.110  →  0.636     (5.8배)
항력 복원            [0.13,0.03] → [0.499,0.299]  (설정값 [0.50,0.30] 일치)
```

**다음**: 내일 실기 비행(F1·F2·F3) → 계수 식별 → 플랜트 정합 → 밴드 재측정 → 학습 → 배포
