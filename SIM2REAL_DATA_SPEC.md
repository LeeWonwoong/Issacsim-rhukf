# 실기체 데이터 수집 스펙 (sim-to-real 준비) — 2026-07-28

대상: PX4 기반 쿼드로터 (질량 1.372kg). 목적은 두 가지다.
1. **실기 캘리브레이션** — sim 과 동일한 UKF 모델을 실기 물성으로 맞춘다.
2. **실기 NIS 분포 확보** — 정책의 입력 분포를 실측해서, sim 의 분포가 여기 겹치도록 조율한다.
   (전이의 최종 판단 기준. 공격은 sim 에서만 하면 되고, 실기는 **평시 비행만** 필요하다.)

> ⚠ 파라미터명은 PX4 버전마다 다를 수 있다. 아래는 확인 후 사용할 것.

---

## 0. 비행 전 설정 (이걸 안 하면 데이터가 반쪽이 된다)

| 항목 | 설정 | 이유 |
|---|---|---|
| 로깅 프로파일 | `SDLOG_PROFILE` 에 **High rate** 켜기 (가능하면 Raw FIFO IMU 도) | 기본 프로파일은 IMU 를 데시메이트해서 진동 분석이 안 된다 |
| 로깅 시작 | `SDLOG_MODE` = 시동 전부터 기록 | 지상 정지 구간이 있어야 센서 기준선을 잡는다 |
| ESC 텔레메트리 | 가능하면 활성화 (§4) | 있으면 sim 과 **동일한 최고정확도 캘리브 방법**을 실기에도 쓸 수 있다. **모터 τ_up/τ_down 분리는 RPM 없이는 불가** (§4-1) |
| **ESC 제어모드** | **배포할 설정으로 먼저 동결** (damped light / active braking 여부) | τ_down 이 여기 좌우된다. 설정을 정하기 전에 재면 잰 값이 배포 기체에 안 맞는다 (§4-1) |
| 기체 상태 | 프로펠러 밸런싱, 진동 마운트 점검 | 진동은 자이로 채널 = 주 탐지 feature 에 직결 |
| 환경 | 바람 약한 날, 고도 **3m 이상** | 지면효과(ground effect) 회피 |
| 배터리 | 매 비행 같은 충전 상태에서 시작 | 추력계수 드리프트를 분리해서 보려면 |

---

## 1. 비행 궤적 (각 60초 내외, 한 배터리에 3~4개씩 나눠 담아도 됨)

### A군 — 캘리브레이션용 (여기 신호가 목적)

| # | 궤적 | 얻는 것 | 요령 |
|---|---|---|---|
| A1 | **정지 호버 60s** | 호버 스로틀 → `C_thrust`, 트림 토크 → **COM 오프셋**, 진동 기준선 | 위치 유지 모드, 최대한 가만히 |
| A2 | **수직 왕복** ±2 m/s, 4~5회 | 추력 채널 여기, `drag_z` | 상승·하강을 또렷이 분리 |
| A3 | **전후 직선 왕복** 3~5 m/s, 각 방향 4회 | `drag_x`, 피치 응답 | 등속 구간을 길게 (항력은 등속에서 보임) |
| A4 | **좌우 직선 왕복** 3~5 m/s | `drag_y`, 롤 응답 | 〃 |
| A5 | **제자리 요 회전** ±90°씩 4회 | `C_torque_z` | 천천히/빠르게 각 2회 |
| A6 | **자세 doublet** (핵심) | `C_torque_x/y` | §3 참고 — 이게 제일 중요하고 제일 놓치기 쉽다 |

### B군 — NIS 분포 대조용 (sim 궤적과 1:1 대응)

| # | 궤적 | sim 대응 |
|---|---|---|
| B1 | 호버 유지 | `hover` 셀 |
| B2 | 사각/직선 웨이포인트 순회 | `waypoint` |
| B3 | 원 궤적 | `circle` |
| B4 | 8자 궤적 | `figure8` |
| B5 | 급기동 (빠른 방향전환 반복) | `aggressive` |

B군은 **속도·고도 프로파일을 sim 과 맞춰야** 분포 비교가 의미 있다. sim 설정값(비행고도, 패턴 크기, 속도)을 먼저 뽑아서 그대로 날릴 것.

---

## 2. 기록할 데이터

### 2-1. ulog (기본 경로 — 이게 가장 확실하고 정보량이 많다)

SD 카드의 `.ulg` 하나면 아래가 전부 들어 있다. 별도 컴패니언 컴퓨터가 필요 없다.

| ulog 메시지 | 쓰임 |
|---|---|
| `vehicle_thrust_setpoint` | 정규화 추력 명령 (sim 과 동일 정의) |
| `vehicle_torque_setpoint` | 정규화 토크 명령 (sim 과 동일 정의) |
| `sensor_combined` | 원시 자이로/가속도 — 캘리브 + **진동 PSD** |
| `vehicle_angular_velocity` | 필터링된 각속도 (제어기가 보는 값) |
| `vehicle_attitude` | 자세 쿼터니언 |
| `vehicle_local_position` | NED 위치/속도 (EKF2 융합 — sim 의 GT 대용) |
| `actuator_motors` | 모터별 정규화 명령 0~1 |
| `esc_status` | **실제 RPM** (텔레메트리 있을 때만) |
| `battery_status` | 전압/전류 → 배터리 새그 |
| `vehicle_imu_status` | 진동 지표 (vibe) |
| `vehicle_air_data` / `sensor_gps` | 참고 |

### 2-2. ROS2 토픽 (컴패니언 컴퓨터 + uXRCE-DDS 쓸 경우)

sim 파이프라인과 **동일한 경로**를 실기에서도 검증하고 싶을 때 추가로 기록한다.
`ros2 bag record` 대상:

```
/fmu/out/vehicle_thrust_setpoint
/fmu/out/vehicle_torque_setpoint
/fmu/out/sensor_combined
/fmu/out/vehicle_odometry
/fmu/out/vehicle_attitude
/fmu/out/vehicle_local_position
/fmu/out/battery_status
/fmu/out/esc_status          ← dds_topics.yaml 에 추가 필요할 수 있음
/fmu/out/actuator_motors     ← 〃
```

⚠ uXRCE-DDS 는 `dds_topics.yaml` 에 등재된 토픽만 내보낸다. `esc_status`,
`actuator_motors`, `vehicle_imu_status` 는 기본에 없을 수 있으니 미리 추가하고 빌드할 것.
(sim 에서 쓰는 thrust/torque setpoint, sensor_combined, odometry 는 이미 설정돼 있다.)

**권장: ulog 를 기본으로 받고, ROS2 bag 은 배포 파이프라인 점검용으로 한 번만.**
캘리브레이션 자체는 ulog 가 레이트·정보량 모두 우수하다.

---

## 3. A6 자세 doublet 이 왜 핵심인가

sim 에서 실제로 겪은 함정이다. 자세 rate loop 는 **폐루프**라서, 평범한 비행 로그로
`ω̇ ~ τ_cmd` 회귀를 돌리면 **롤/피치 계수가 14배까지 틀리게 나온다** (실측 0.265 vs 정답 3.57).
명령이 상태의 함수라 회귀가 편향되기 때문이다. 해법은 **명령을 상태와 무관하게 흔드는 것**:

1. **수동 doublet (가장 간단, 권장)** — Stabilized/Acro 모드에서 롤 스틱을 짧게
   `+ → − → 중립` 으로 톡 치기. 0.3~0.5초 폭으로 5~10회. 피치도 동일. 고도 여유 충분히.
2. **PX4 멀티콥터 오토튠** — 자세 루프에 여기 신호를 자동 주입하는 기능. 그 비행 로그를
   그대로 쓰면 된다. (활성화 파라미터명은 버전 확인 필요)
3. **ESC 텔레메트리가 되면 불필요** — 실제 RPM 을 알면 §4 처럼 동역학을 아예 안 거치고
   계수를 뽑을 수 있다.

---

## 4. ESC 텔레메트리란 무엇이고 왜 중요한가

**무엇인가**: 보통 ESC 는 명령을 받기만 하고 아무것도 되돌려주지 않는다(개루프). 텔레메트리가 되면
ESC 가 **실제 회전수(eRPM)** 와 전압/전류/온도를 비행컨트롤러로 되돌려준다. 두 방식이 있다.

- **양방향 DShot (bidirectional DShot)** — 기존 신호선 하나로 RPM 을 되돌려준다. **배선 추가 없음.**
  BLHeli_32 / AM32 계열 펌웨어면 대개 지원한다.
- **텔레메트리 UART (KISS/BLHeli telemetry)** — ESC 의 별도 TX 선을 FC 의 UART 에 연결.
  RPM + 전압/전류/온도까지 나온다.

PX4 에서 켜면 로그에 `esc_status` 가 남고 RPM 필드가 채워진다.
(파라미터명은 버전마다 다르다 — DShot 텔레메트리 UART 지정 / 양방향 DShot enable 계열을 확인할 것)

**왜 중요한가**: 오늘 sim 에서 쓴 **최고 정확도 캘리브 방법이 그대로 실기에 이식된다.**

```
RPM 을 알면 →  각 로터 추력 T_i = k·ω_i²  →  적용 힘/토크를 직접 계산
            →  "명령 → 실제 적용" 정적 매핑만 적합 = 동역학·폐루프 편향 원천 배제
```

sim 에서 이 방법이 정답(3.568 / 4.017 / 4.907)을 냈고, 폐루프 회귀는 14배 틀렸다.
실기에서 텔레메트리가 없으면 그 **틀린 쪽 방법에 의존**해야 하므로, 가능하면 켜는 걸 강력히 권한다.

로터 상수 `k` 는 정지추력 시험대(로드셀)로 재거나, 호버 평형 `4k·ω_hover² = m·g` 에서 역산한다.

### 4-1. 모터 지연 τ — RPM 계단응답에서 읽되, 주의 3가지

sim 의 `run_sim.py MOTOR_TAU` 에 넣을 값이다. 그냥 하나 재서 넣으면 안 되는 이유:

1. **스핀업과 스핀다운의 τ 가 다르다.** 모터는 토크로 올리지만, 내릴 때는 프로펠러 공력항력에만
   의존한다(ESC 액티브 브레이킹이 없으면). 보통 **τ_down ≈ 2~3 · τ_up**.
   → 이 프로젝트에 직결된다: bias 주입은 **일부 로터 up / 일부 down** 이고,
     온셋 엣지(t=1~2 스파이크 = RL 정당화의 핵심 근거)는 **느린 쪽이 지배**한다.
     대칭 τ 하나로는 온셋 모양이 실기와 다르다. 상승/하강을 **따로 적합**할 것.
2. **⚠ ESC 설정을 먼저 동결하고 재라.** τ_down 은 ESC 제어모드(damped light / active braking)에
   좌우된다. 설정을 정하지 않고 재면, 잰 τ 가 실제 배포 기체에 안 맞는다.
3. **τ 는 RPM 의존이다.** 선형화하면 `τ ≈ J_prop / (2·c_d·ω₀)` — RPM 이 높을수록 짧아진다.
   상수 τ 는 한 동작점의 선형화이므로 **호버 RPM 근방에서 동정**할 것
   (평시 NIS 기준선을 지배하는 동작점이 호버이므로).

---

## 5. 수집 후 분석 순서

### 5-A. 캘리브레이션 (모델 계수)
1. **A1** → 호버 스로틀·트림 토크 → `C_thrust`, COM 오프셋 실측
2. **A2~A5** → `drag`, `C_torque_z`
3. **A6** (또는 ESC RPM) → `C_torque_x/y` ← 편향 없는 경로로

### 5-B. ★ NIS 분포 정합 — 레버 우선순위 (이 순서가 중요하다)

최종 판정 기준은 **"계수가 맞는가"가 아니라 "실기 NIS 분포와 sim NIS 분포가 겹치는가"** 다.
정책이 먹는 게 NIS 이기 때문. 그 분포를 움직이는 노브를 **영향 큰 순서**로 적는다.

| 순위 | 노브 | 어느 채널을 지배하나 | 실기 측정 |
|---|---|---|---|
| **1** | **GPS 속도 노이즈 σ** | **`nis_vel` 직접** | `sensor_gps` 속도 vs RTK(또는 EKF2) 잔차의 σ |
| **2** | **자이로 진동 노이즈 σ·대역** | **`nis_gyro` 직접** | `sensor_combined` PSD |
| 3 | 모터 지연 τ_up / τ_down (§4-1) | 온셋 엣지의 **모양** | `esc_status` RPM 계단응답 |
| 4 | 배터리 새그 | 에피소드 내 기준선 **드리프트** | `battery_status` 전압 vs 호버 스로틀 |

> **왜 GPS 가 1순위인가**: UKF 관측은 Isaac GT 가 아니라 **PX4 `sensor_gps` 의 속도**다
> (`online_rl_main.py:305` → `:818` → `ukf.step`). 구조는 실기에 그대로 이식되지만,
> 그래서 `nis_vel` 은 **GPS 속도 잔차**이고 그 크기를 지배하는 건 GPS 노이즈 모델이다.
> Pegasus GPS 노이즈가 실기(도플러 기반, 통상 σ≈0.05~0.1 m/s)와 다르면
> **모터 τ 를 아무리 정확히 맞춰도 `nis_vel` 분포가 통째로 어긋난다.** d′ 2.2 를 담당하는 채널이 이것.
> 자이로도 같다 — 실기 프로펠러 진동은 sim σ=0.031 보다 통상 훨씬 크다.
> 배터리 새그는 sim 에 **현재 미구현**이다(넣을지 결정 필요).

### 5-C. 대조 및 마무리
7. **B1~B5** → 실기 캘리브로 UKF 오프라인 적용 → **실기 NIS 분포**
8. sim 캡처의 NIS 분포와 대조 → **5-B 노브를 1순위부터** 조율해 겹치게 만듦 → 이후 학습

## 6. 최소 세트 (시간이 없다면)

`A1(호버 60s)` + `A6(doublet)` + `B5(급기동)` 세 개면 시작할 수 있다.
- **A1: 가성비가 압도적이다** — C_thrust, COM 오프셋, 진동 기준선(자이로 σ)에 더해,
  **정지 호버에서는 참 속도 ≈ 0 이므로 `sensor_gps` 속도가 곧 GPS 속도 노이즈**다.
  즉 5-B 의 1·2 순위 노브를 이 60초 하나로 둘 다 잰다. RTK 없이도 된다.
- A6: C_torque_x/y (편향 없이)
- B5: NIS 분포 상단(가장 까다로운 영역) 대조

여기에 **ESC 텔레메트리**가 켜져 있으면 A1 만으로 모터 τ 까지 나온다(호버 RPM 근방 미세 계단).
