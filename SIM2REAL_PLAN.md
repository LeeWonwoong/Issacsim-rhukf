# sim-to-real 실행 계획 — 무엇을 재고, 무엇을 맞추고, 무엇으로 판정하는가

> 2026-07-29 작성. Phase 0 실측 결과 반영.
> 데이터 **수집 방법**(궤적·ulog 메시지·ROS2 토픽·ESC 텔레메트리)은 `SIM2REAL_DATA_SPEC.md` 참조.
> 이 문서는 **순서·계산식·판정 기준·결정 게이트**를 다룬다.

---

## 0. 한 줄 요약

**최종 목표는 학습된 정책을 실기 PX4 기체에 올려 online inference 하는 것이다(논문 필수 요소).**

**맞춰야 할 것은 파라미터가 아니라 "정책이 먹는 입력 분포"(NIS)다.**
그리고 NIS 를 지배하는 것은 센서 노이즈가 아니라 **남아 있는 모델오차**다.
오늘 그 증거를 얻었다 — 항 하나(추력-토크 결합)를 빼먹었더니 평시 자이로 NIS 가 **485배** 부풀었다.

### 전체 트랙
```
[sim 트랙]  P1 기준선+바람 → ★관측 정규화 결정 → P2 밴드 → P3 데드라인
            → P4 분리도 → 강건성 스윕(비행 전 검증) → P5 학습 → 배포 후보 정책

[실기 트랙 — 병렬 필수, 리드타임 김]
            ESC 제어모드 동결 → 텔레메트리 배선
            컴패니언 컴퓨터 + uXRCE-DDS 파이프라인
            ★ PX4 공격 주입 모듈 (커스텀 빌드, §2-3)
            A1 호버 60s → 계수 + 노이즈   A6 doublet → C_torque
            B1~B5 → 평시 NIS 분포

[합류]      실기 NIS 분포 vs sim 대조 → 정규화 파라미터 확정
            → 단계적 비행 시험 (밴드 아래 → 밴드 내)
```

---

## 1. 오늘 확인된 것이 계획을 어떻게 바꿨나

### 1-1. ★ 모델오차 한 항이 NIS 바닥 전체를 지배한다

| 조건 | `nis_g_raw` 중앙 | `nis_v_raw` 중앙 |
|---|---|---|
| 모터지연 ON, 결합항 없음 | 12.180 | 0.527 |
| 모터지연 OFF, 결합항 없음 | 2.910 | 0.164 |
| **모터지연 OFF, 결합항 포함** | **0.006** | **0.031** |

메커니즘: Iris 는 로터 중심이 COM 에서 (x+6.7mm, y−1.9mm) 어긋나 총추력이 상시 토크를 만든다.
PX4 는 트림 토크로 상쇄하는데(호버에서 `u2_ty` 중앙 −0.0858 N·m), UKF 모델에 결합항이 없으면
**그 트림을 실제 토크로 오해**해 ω̇y −3.24 rad/s² 를 예측한다. 자이로 칼만이득이 약해(|K| 행합 0.041)
상태가 안 당겨지고 정상상태 잔차 2.07 rad/s 로 눌러앉는다 → NIS 바닥이 통째로 올라간다.

**교훈: 실기 정합에서 "센서 노이즈부터 맞추자"는 순서가 틀렸다. 모델오차 항 목록부터 닫아야 한다.**
(이 문서 §3 이 그 목록이다.)

### 1-2. ★ 파라미터를 맞추는 게 아니라 폐루프 응답을 맞춰야 한다

모터 1차 지연 τ_up=30ms / τ_down=70ms 를 sim 에 주입했더니 **자세 루프가 발진**했다:

| 항목 | τ=0 | τ=0.03/0.07 | 배율 |
|---|---|---|---|
| \|gyro_x\| 중앙 | 0.0107 | 1.0693 | 100× |
| \|gyro_z\| 중앙 | 0.0378 | 2.1574 | 57× |
| \|u3_tz\| 95pct | 0.0593 | 2.4679 | 42× |

원인은 τ 자체가 아니라 **PX4 게인이 지연 없는 Pegasus 플랜트에 맞춰져 있다**는 것.
그런데 **실기는 지연이 있는 채로 안정적으로 난다** — 실기 PX4 게인이 그 지연에 맞춰 튜닝돼 있기 때문이다.

> 즉 sim(τ=0, 게인 A)과 real(τ=30/70ms, 게인 B)은 **각각 내부적으로 일관**되어 있다.
> τ 만 sim 에 옮기는 것은 두 시스템 중 어느 쪽도 아닌 제3의 불안정한 플랜트를 만든다.

**따라서 정합 대상은 τ 가 아니라 폐루프 자세 응답(대역폭·감쇠)이다.** 둘 중 하나를 택한다:
- **(A) 응답 정합** — 실기 rate loop 계단/doublet 응답을 재고, sim 이 같은 대역폭·감쇠를 갖도록
  (τ, 게인) **쌍**을 맞춘다. 정확하지만 sim 플랜트를 바꾸므로 밴드 재측정을 유발한다.
- **(B) τ=0 유지 + 차이를 명시** — sim 은 Pegasus 기본(지연 없음)으로 두고, 실기와의 응답 차이를
  한계로 기록한다. 밴드 재측정 불필요. **현재 기본 선택.**

판단 기준: 실기 rate loop 대역폭이 sim 과 **2배 이내**면 (B), 그 이상 벌어지면 (A). §4-3 참조.

### 1-3. 실기에서 못 맞춘다던 항이 사실은 맞출 수 있다

07-28 에 결합항을 뺀 명분은 "실기는 페이로드마다 COM 이 달라 맞출 수 없다" 였다. **틀렸다.**
이 항은 **호버 트림 토크에서 직접 나눗셈 한 번으로 나온다**(§3-4). 기체가 바뀌어도 호버 60초면 재추정된다.

> 일반 원칙: "실기에서 못 맞춘다"고 판단하기 전에 **호버 정지 상태에서 읽히는 값인지** 먼저 확인할 것.
> 상시 편향(constant bias)은 거의 다 호버에서 읽힌다.

---

## 2. 최종 목표 = 실기 PX4 기체에서 online inference (논문 필수 요소)

> ⚠ **2026-07-29 정정**: 이 문서 초판은 "본 기여가 sim 내부 비교이므로 실기 데이터는
> 크리티컬 패스가 아니다" 라고 썼다. **틀렸다.** 사용자 목표는 학습된 정책을 실기 PX4
> 기체에 올려 **online inference 까지 수행**하는 것이고, 이것이 논문의 필수 요소다.
> → 실기 트랙은 **선택이 아니라 병렬 필수 트랙**이며, 리드타임이 길어 지금 착수해야 한다.

### 2-1. 그래도 sim 이 먼저다
배포할 정책을 먼저 학습시켜야 하므로 sim 트랙(P1→P5)의 순서는 그대로다.
실기는 **학습된 정책이 나온 뒤**에 의미가 생긴다. 실기 트랙은 그 사이 병렬로 준비한다.

### 2-2. 항목별 필요도 (수정)

| 항목 | 실기 데이터 | 비고 |
|---|---|---|
| RHUKF-FV vs Adam 샘플효율 | 불필요 | sim 내부 비교 (양쪽이 같은 환경) |
| 공격 밴드 / 데드라인 / basin | 불필요 | sim 플랜트 성질 |
| 관측 정의 (12D/16D, 압축) | 불필요 | sim 분리도로 판정 |
| **NIS 분포 정합** | **필수** | 정책 입력 분포가 어긋나면 실기 추론이 무의미 |
| **모델오차 항 목록 닫기** | **필수** | §1-1 |
| **UKF 계수 실기 캘리브** | **필수** | 기체별로 다시 잰다 |
| **폐루프 자세 응답** | **필수** | §1-2. 배포 대상 기체 기준 |
| **★ 실기 공격 주입 수단** | **필수** | §2-3 — 리드타임 큰 신규 항목 |

### 2-3. ★ 실기 공격 주입 — 가장 큰 미해결 항목

sim 은 `bias_scale` 을 `cmd_torque` 에 곱해 주입한다. 실기에서 **같은 주입점**을 재현하려면
자세 컨트롤러 출력과 믹서 사이를 가로채야 한다.

| 방법 | 평가 |
|---|---|
| **PX4 펌웨어에 주입 모듈 추가** (`vehicle_torque_setpoint` 에 bias 가산) | **정석.** sim 과 동일 주입점. 커스텀 빌드 필요 |
| 오프보드 `ActuatorMotors` 직접 제어 | 컨트롤러를 우회 → **공격의 의미가 달라진다** |
| 물리적 고장(프로펠러 손상 등) | 제어·재현 불가 |

→ **커스텀 PX4 빌드가 필요하다.** 별도 리드타임 항목으로 잡을 것.

**안전 — 밴드의 정의가 "무대응 시 추락" 이다.** 밴드 내 공격을 주면 탐지+호버 전환이
첫 시도에 성공해야 기체가 산다. 단계적 상승 필수:
```
1단계  밴드 아래(비결과성) 공격  → 탐지만 확인, 추락 위험 없음
2단계  밴드 하단 + 고고도 + 넓은 공터 + 안전조종사
3단계  밴드 내
```

### 2-4. 관측 정규화가 선택 → 필수로 승격

실기 배포가 목표이므로, sim↔real NIS 격차를 흡수하는 주 메커니즘이 필요하다.
**비행 초반 기준선으로 NIS 를 정규화**해서 정책이 baseline 불변성을 **학습하게** 하는 쪽이,
배포 시점의 사후 상수 보정보다 낫다.

⚠ 사후 상수 보정의 한계 (압축이 비선형이라 생기는 문제):
`ε̃ = ln(1+ε)/(1+ln(1+ε))` 에서 같은 세기의 공격(raw +5)이 바닥에 따라
```
  바닥 0.006 → scaled 0.0059 → 공격 0.642   대비 0.636
  바닥 2.9   → scaled 0.577  → 공격 0.686   대비 0.110   ← 동적범위 5.8배 손실
```
**압축 뒤에 상수를 빼도 복구되지 않는다.** 보정하려면 **raw NIS 단계**에서 해야 하고,
그마저도 (a) 명령 비례 오차 (b) 분포 꼬리 모양 (c) 윈도우 내 시간 상관 은 못 잡는다.
→ 관측 정의에 넣는 것이 정답. **학습 전 결정 항목이므로 P1 직후가 마지막 시점.**

### 2-5. 강건성 스윕의 역할 (수정)
실기 데이터의 *대체재*가 아니라 **비행 전 사전검증**이다.
"이 정책이 캘리브 불일치 ±10% 범위를 견디는가" 를 확인하고 나서 날린다.
견디지 못하면 날려도 실패한다.

---

## 3. 모델오차 항 목록 — 닫아야 할 것 전부

UKF 예측 `_f()` 가 쓰는 항을 하나씩 세운다. 각 항마다 **실기에서 어떻게 읽는지**를 명시한다.
sim 확정값은 오늘 기준이며, 실기 값이 이것과 다른 것은 정상이다 — **절차가 같은지**가 중요하다.

### 3-1. 질량 · 관성
- **sim 확정**: m = 1.372 kg, I = (0.029547, 0.026484, 0.053359) kg·m²
- **실기 측정**: 저울(AUW, 배터리·페이로드 포함). 관성은 이중진자(bifilar pendulum) 또는 CAD.
- **함정**: sim 에서 Iris USD 는 body 1.5 kg + **로터 4개 0.1186 kg** 이라 실제 1.6186 kg 였다.
  "기본값"을 믿지 말고 런타임 쿼리할 것(`probe_mass.py`).
  실기 대응 함정: 프로펠러·배터리·짐벌을 빼먹은 AUW.

### 3-2. C_thrust (정규화 추력 → N)
- **sim 확정**: 25.58 N (호버 u_norm 0.5274, T/W 3.07)
- **실기 측정**: 호버 평형. `C_thrust = m·g / u_hover`
  - `u_hover` = `vehicle_thrust_setpoint.xyz[2]` 절댓값의 정지 호버 평균
- **검증**: 로드셀 정지추력 시험대가 있으면 대조. 없어도 호버 평형만으로 충분하다.
- **주의**: 추력은 원래 2차 곡선이다. 선형 C_thrust 는 **호버점 접선(secant)** 이고 질량이 바뀌면 같이 바뀐다.
  sim 기준: secant 25.5 vs 국소기울기 42.9 — **1.7배 차이**. 급기동 구간 잔차 증가는 구조적으로 정상.

### 3-3. C_torque (정규화 토크 → N·m) ★ 가장 틀리기 쉬움
- **sim 현재**: x=3.568, y=4.017, z=4.907 — **y 는 검증 중(물리 상한 초과, §6)**
- **실기 측정**: 우선순위 순서대로
  1. **ESC 텔레메트리(양방향 DShot)** 가 되면 최선 — 실제 RPM 으로 적용 토크를 직접 계산
     `τ_roll = Σ y_i·k·ω_i²`, `τ_pitch = Σ (−x_i)·k·ω_i²` → 정규화 명령에 회귀
     동역학·관성·폐루프가 전혀 안 들어가는 **정적 매핑**이라 가장 신뢰도 높다.
  2. 텔레메트리가 없으면 **자세 doublet**(Acro/Stabilized 에서 스틱 톡톡, 0.3~0.5s 폭 × 5~10회)
     또는 PX4 오토튠 로그. **평범한 비행 로그로 `ω̇ ~ τ_cmd` 회귀를 돌리면 안 된다** — 폐루프 편향.
- **★ 반드시 할 검산 — 기하학적 상한**:
  ```
  τ_roll_max  = Σ over rotors:  (+y 쪽은 T_max, −y 쪽은 T_min) × y_i
  τ_pitch_max = 같은 방식으로 x_i 사용
  T_max = k·ω_max²  (로터 1개 최대추력)
  ```
  sim 값: 롤 4.321 N·m / 피치 2.555 N·m, 암 길이 0.2126 / 0.1313 m.
  **적합값이 이 상한을 넘으면 물리 위반이다 — 데이터와 무관하게 틀렸다.**
  또한 **롤/피치 비가 암 길이 비와 맞아야 한다**(sim: 1.69 vs 1.62 ✓).
  현재 sim 의 C_x/C_y = 0.888 로 **역전**돼 있어 재적합 중이다(§6).
- **함정**: C_torque 는 관성 I 와 **짝**이다. I 를 바꾸면 반드시 재적합.

### 3-4. 추력-토크 기하 결합 K ★ 오늘 추가된 필수 항
- **sim 확정**: K = [−0.00121, +0.00621, +0.00013] N·m per N
- **실기 측정**: **정지 호버 60초 평균만 있으면 된다.**
  ```
  K[i] = mean(vehicle_torque_setpoint[i] × C_torque[i]) / mean(|vehicle_thrust_setpoint[2]| × C_thrust)
  ```
  즉 "호버에서 PX4 가 상시 걸고 있는 트림 토크 ÷ 총추력".
- **검산**: 적용 후 호버 트림이 0 으로 상쇄되는지.
  sim 실측: −0.0858 N·m + (0.00621 × 13.485 N) = **−0.002 ≈ 0** ✓
- **왜 중요한가**: 빼먹으면 NIS 바닥이 485배 부푼다(§1-1). **최우선 항.**
- 페이로드/배터리 위치를 바꾸면 재측정(호버 60초).

### 3-5. 항력 (drag)
- **sim 확정**: [0.5, 0.3, 0.0] (Pegasus `LinearDrag` 설정값과 일치 확인)
- **실기 측정**: 등속 직선 비행에서 `F_drag = T_horizontal − m·a`. 축별로(전후/좌우) 왕복.
  등속 구간을 길게 잡을 것 — 가속 구간에서는 안 보인다.
- **활용**: 이 값은 **프레임 정합의 리트머스 시험지**이기도 하다(§7-2).

### 3-6. 모터 지연 τ (선택 — §1-2 참조)
- **sim 현재**: **비활성**(Pegasus 기본)
- **실기 측정**: ESC RPM 계단응답. **상승/하강을 따로** 적합(τ_down ≈ 2~3·τ_up).
  **호버 RPM 근방**에서 잴 것 — τ ≈ J_prop/(2·c_d·ω₀) 로 RPM 의존이다.
  ⚠ **배포할 ESC 제어모드(damped light / active braking)를 먼저 동결**하고 잴 것.
- **넣을지는 §4-3 게이트로 판단.** 그냥 넣으면 발진한다.

### 3-7. 배터리 새그 (미구현)
- **sim**: 없음. 전압 강하에 따른 추력 감소가 모델링되지 않음.
- **실기 측정**: `battery_status` 전압 vs 호버 스로틀 드리프트율.
- **영향**: 에피소드 내 NIS 바닥의 **느린 상승**. 밴드 측정에는 영향 작으나 긴 비행에서 오탐 요인.
- **판단**: §4 의 NIS 대조에서 실기 NIS 가 시간에 따라 드리프트하면 그때 넣는다.

---

## 4. NIS 분포 정합 — 최종 판정 절차

### 4-1. 목표
정책이 먹는 것은 `[nis_vel, nis_gyro, action] × window4`. 따라서
**"실기 평시 NIS 분포가 sim 평시 NIS 분포와 겹치는가"가 유일한 최종 판정 기준**이다.
개별 계수가 얼마나 정확한지는 그 자체로는 목표가 아니다.

### 4-2. sim 쪽 기준값 (2026-07-29 확정, bias=0 · 외란 none · 모터지연 OFF · 결합항 포함)

| 지표 | 중앙값 |
|---|---|
| `nis_v_raw` | 0.031 |
| `nis_g_raw` | 0.006 |
| `nis_v_scaled` | 0.0297 |
| `nis_g_scaled` | 0.0061 |

패턴별 `nis_g_raw`: hover 0.009 / waypoint 0.005 / circle 0.018 / figure8 0.003 / aggressive 0.001
측정 자이로 중앙 |ω|: x 0.0085 / y 0.0093 / z 0.0389 rad/s (센서 노이즈 σ≈0.031 수준)

> ⚠ 이 값들은 **바람 없음** 기준이다. 실기는 무풍일 수 없으므로 대조 시 sim 도
> `wind_turbulence` 를 켜서 같은 조건으로 비교하거나, 실기의 무풍에 가까운 구간만 골라야 한다.

### 4-3. 절차

```
1) 실기 A군 비행 (캘리브레이션용) → §3 의 계수 전부 산출
   ├ A1 호버 60s   → C_thrust, 결합항 K, 자이로 진동 σ, GPS 속도 노이즈 σ  ← 가성비 최고
   ├ A2~A5         → drag, C_torque_z
   └ A6 doublet    → C_torque_x/y  (+ 기하 상한 검산 필수)

2) 계수 검산 게이트 — 하나라도 실패하면 3) 로 넘어가지 말 것
   □ C_torque 가 기하 상한 이내인가
   □ 롤/피치 비가 암 길이 비와 맞는가
   □ 결합항 적용 후 호버 트림이 0 으로 상쇄되는가
   □ 항력 적합값이 물리적으로 타당한가 (§7-2)

3) 실기 B군 비행 (평시 분포 대조용) → sim 궤적과 1:1 대응
   B1 호버 / B2 웨이포인트 / B3 원 / B4 8자 / B5 급기동
   ※ 속도·고도·궤적 크기를 sim 설정과 맞출 것. 안 맞으면 분포 비교가 무의미하다.

4) B군 로그에 **오프라인으로 UKF 를 적용** → 실기 NIS 분포 산출
   (실기에서 UKF 를 실시간 돌릴 필요 없다. ulog 를 그대로 먹이면 된다.)

5) sim NIS 분포와 대조 → 어긋나면 아래 순서로 조정
```

### 4-4. 어긋났을 때 조정 순서 (영향 큰 순)

| 순위 | 노브 | 지배 채널 | 실기 측정 |
|---|---|---|---|
| **1** | **모델오차 항 누락** (§3) | 양쪽 다 | 계수 검산 게이트 |
| **2** | GPS 속도 노이즈 σ | `nis_vel` | A1 호버에서 참속도≈0 이므로 GPS 속도가 곧 노이즈 |
| **3** | 자이로 진동 σ·대역 | `nis_gyro` | `sensor_combined` PSD |
| 4 | 폐루프 자세 응답 | `nis_gyro` | rate loop 대역폭 — **2배 넘게 벌어지면 §1-2 (A) 채택** |
| 5 | 배터리 새그 | 느린 드리프트 | `battery_status` |

> **1순위가 압도적이다.** 오늘 결합항 하나가 485배를 만들었다. 2~5 는 그 다음이다.
> 07-28~29 이전 계획은 2·3 을 1순위로 뒀었는데, 그건 틀린 순서였다.

### 4-5. 판정 기준 (제안)

- **통과**: 실기와 sim 의 `nis_v_scaled` / `nis_g_scaled` **중앙값이 2배 이내**,
  **90pct 가 3배 이내**. (scaled 는 [0,1] 압축이라 raw 보다 관대한 기준이 맞다.)
- **부분 통과**: 한 채널만 맞음 → 그 채널만 쓰는 축소 관측을 고려하거나, 어긋난 채널의 노브를 재조정.
- **실패**: 두 채널 다 크게 어긋남 → §3 항 목록에 **빠진 항이 더 있다**. 4-4 의 1순위로 돌아갈 것.

---

## 5. 캘리브레이션 절차의 전이성 증명 (실기 비행 전에 sim 에서 먼저)

오늘 sim 에서 쓴 "로터 각속도 → 적용 토크" 경로는 **sim 전용**이다(실기는 ESC 텔레메트리 필요).
실기에서 텔레메트리가 없으면 doublet/오토튠 경로를 써야 하는데, **그 경로가 정답을 복원하는지
sim 에서 먼저 증명**해둘 수 있다. sim 에는 ground truth 가 있기 때문이다.

```
sim ground truth (로터 경로)          : C_torque_x/y/z
sim 에서 doublet 경로로 재적합         : 같은 값이 나오는가?
  ├ 나온다 → 실기에서 텔레메트리 없이도 신뢰 가능
  └ 안 나온다 → 실기에서도 텔레메트리가 사실상 필수
```

이미 알려진 반례: `calibrate_sysld.py`(가속도계 기반 폐루프 회귀)는
C_thrust·drag·C_torque_z 는 맞히지만 **C_torque_xy 는 14배 틀린다**. 폐루프 편향 때문.
→ 실기용 절차는 반드시 **개루프 여기**(doublet 또는 치프 주입)를 포함해야 한다.

---

## 6. 현재 열려 있는 항목

| 항목 | 상태 |
|---|---|
| `C_torque_y` 물리 상한 157% 초과 | ROTOR_LOG 캡처 + 독립 검증 큐 대기 중 (`run_ctorque_when_free.sh`) |
| 모터 지연 (A)응답정합 vs (B)τ=0 유지 | 실기 rate loop 대역폭 측정 후 판단. 현재 (B) |
| 배터리 새그 | 실기 NIS 시간 드리프트 확인 후 판단 |
| 바람 조건에서의 NIS 대조 | sim 바람 재측정(P1) 후 |

---

## 7. 함정 모음 (실제로 밟은 것들)

### 7-1. "기본값"을 믿지 말 것
Iris USD 의 "1.5 kg" 은 **바디만**의 값이고 로터 4개(0.1186 kg)가 별개 강체다.
실제 비행질량은 1.6186 kg 였다. **런타임 쿼리로 확인할 것.**

### 7-2. 프레임 규약 — 항력이 리트머스 시험지
Isaac GT 쿼터니언은 **ENU 관성 / FLU 바디**다. 표준 ZYX 로 얻은 각을 NED 인 양 쓰면
추력벡터를 틀린 수평 방향으로 돌린다. 조용히 망하는 버그다.
**무료 검증법**: 항력을 적합해서 알려진 설정값이 복원되는지 본다.
sim 실측 — 프레임 틀렸을 때 [0.13, 0.03] / corr −0.30, 고친 뒤 [0.499, 0.299] / corr **−1.00** ✓
실기에도 그대로 적용된다: 항력 적합값이 축별로 말이 되는지(전후 > 좌우 등) 확인.

### 7-3. 폐루프 회귀를 쓰지 말 것 (롤/피치 한정)
자세 rate loop 는 폐루프라 `ω̇ ~ τ_cmd` 회귀가 편향된다(R² 0.03~0.15, 계수 2배 요동).
**요·추력·항력은 폐루프 로그로도 괜찮다. 롤/피치만 안 된다.**

### 7-4. 상시 편향은 호버에서 읽힌다
"실기에선 못 맞춘다"고 판단하기 전에 호버 정지 상태에서 읽히는 값인지 확인할 것.
결합항이 이 함정의 실례다 — 못 맞춘다고 판단해 뺐다가 NIS 바닥을 485배 부풀렸다.

### 7-5. 파라미터 이식 ≠ 거동 이식
τ 만 옮기면 sim 도 real 도 아닌 제3의 불안정한 플랜트가 된다(§1-2).
**(파라미터, 게인) 쌍**을 함께 옮기거나, 아예 옮기지 말고 차이를 기록할 것.

### 7-6. 분석 스크립트의 각도 처리
요 차이를 볼 때 `np.unwrap` 없이 계산하면 ±180° 경계에서 358° 같은 값이 나온다.
`verify_calibration.check_yaw` 는 unwrap 을 쓴다. 새 스크립트를 쓸 때도 반드시.

---

## 8. 최소 실행 세트

시간이 없으면 이것만으로 시작할 수 있다.

```
□ ESC 제어모드 동결 (damped light / active braking 여부 결정)
□ 로깅 설정: SDLOG_PROFILE High rate, 시동 전부터 기록
□ A1  정지 호버 60초        ← C_thrust + 결합항 K + 자이로 σ + GPS 속도 σ 를 한 번에
□ A6  자세 doublet          ← C_torque_x/y (기하 상한 검산 필수)
□ B5  급기동                ← NIS 분포 상단(가장 까다로운 영역) 대조
```

**A1 의 가성비가 압도적이다.** 정지 호버에서는 참 속도 ≈ 0 이므로
`sensor_gps` 속도가 **곧 GPS 속도 노이즈**다 — RTK 없이도 §4-4 의 2·3 순위를 둘 다 잰다.
ESC 텔레메트리까지 켜져 있으면 A1 하나로 모터 τ 까지 나온다.

---

## 9. 관련 파일

| 파일 | 용도 |
|---|---|
| `SIM2REAL_DATA_SPEC.md` | 데이터 수집 스펙 (궤적·ulog 메시지·ROS2 토픽·ESC 텔레메트리 설명) |
| `probe_mass.py` | Isaac 런타임 질량/관성 직접 쿼리 |
| `calibration/fit_static_from_rotor.py` | 로터 각속도 기반 C_thrust/C_torque 적합 |
| `verify_ctorque_bounds.py` | **C_torque 기하 상한 검산 + 독립 재적합** (실기 데이터에도 그대로 적용 가능) |
| `calibration/fit_gains.py` | 병진·항력 적합 |
| `verify_calibration.py` | 요 슬루·항력·호버점 회귀검증 |
| `validate_by_regime.py` | 호버/순항/급기동 영역별 고정계수 예측잔차 (`--fit-coupling`) |
| `diag_nis_floor.py` | **평시 NIS 바닥의 축별 원인 분해** — 실기 NIS 가 높을 때 첫 진단 도구 |
| `analyze_p0_flightcheck.py` | 생존율·자세분포·요 슬루·NIS 기준선 판독 |
| `compare_p0_tau.py` | 조건 간 요 진동/NIS 비교 |

⚠ 시스템 python3 는 numpy2/scipy 불일치 → 분석 스크립트는 `~/isaacsim/python.sh` 로 실행.
